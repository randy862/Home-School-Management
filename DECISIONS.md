# Decision Log

| Date | Decision | Rationale | Impact |
|---|---|---|---|
| 2026-03-07 | Use lightweight static web scaffold first | Enables rapid UI iteration before backend commitments | Faster MVP discovery and lower setup friction |
| 2026-03-07 | Adopt 5-agent operating model | Separates planning, delivery, and quality ownership | Clear accountability and parallel workstreams |
| 2026-03-07 | Keep ops artifacts in repo root and role content in dedicated folders | Reduces navigation overhead and onboarding time | Consistent collaboration pattern |
| 2026-03-07 | Model course lengths as hours per instructional day for MVP | Allows direct calculation of total/complete instructional hours from attendance and calendar days | Delivers hour indicators without backend dependencies |
| 2026-03-07 | Keep app as static SPA with local storage for phase 1 | Avoids backend dependency while validating workflow fit | Faster end-to-end prototype delivery |
| 2026-03-07 | Generate calendar schedules from plan definitions instead of storing every event | Reduces data duplication and keeps calendar aligned to plan edits | Calendar updates immediately on plan changes |
| 2026-03-09 | Introduce SQL Server Express migration track with staged cutover | User requested full MSSQL backend migration with data preservation | Added backend/schema/import scaffolding and migration operations docs |
| 2026-03-09 | Keep string IDs during migration phase instead of immediate UUID rewrite | Existing frontend state uses generated string IDs; preserving keys reduces migration risk | Enables low-risk import and incremental backend adoption |
| 2026-03-10 | Add PowerShell-based import fallback script (`server/scripts/import-state.ps1`) | `node`/`npm` unavailable in execution environment, but migration needed to continue | Enabled successful JSON-to-MSSQL import and integrity verification without Node runtime |
| 2026-03-10 | Add persisted system users inside app state instead of a separate auth service | Current app is a single-file SPA with full-state persistence already in place; this keeps the feature aligned with the existing architecture | Login, role gating, and password management now survive reloads and SQL-backed sync |
| 2026-03-10 | Bootstrap a default admin account (`admin` / `ChangeMe123!`) for existing installs | Existing deployments have no user accounts, so auth needs a deterministic first-login path without blocking on manual DB seeding | App can be upgraded in place; admin must change the default password after initial login |
| 2026-03-10 | Restrict student access at the UI/session layer to dashboard, schedule, attendance, and grades | User requested student read-only access limited to those views | Student sessions are filtered to their linked record and cannot submit admin mutations through normal app flows |
| 2026-03-13 | Treat "overall average" as the weighted average of the current filtered test set | Dashboard, Student detail, and Grades had diverged by averaging quarters/courses differently | Overall averages now match across pages when filters represent the same data scope |
| 2026-03-25 | Treat the current MSSQL/full-state backend as a transitional bridge, not the hosted end-state architecture | The long-term goal is a subscription SaaS deployed on Debian with Apache and PostgreSQL, and the current bridge architecture is not the right production boundary | Planning now centers on backend auth, domain APIs, PostgreSQL runtime persistence, and hosted deployment readiness |
| 2026-03-25 | Reach a production-ready single-tenant deployment before implementing multi-tenancy | Multi-tenancy built on browser-owned auth and full-state sync would increase risk and rework | Delivery sequence now prioritizes secure hosting foundations first, then tenant-aware architecture |
| 2026-03-25 | Use PostgreSQL database-per-tenant as the target SaaS isolation model | The requested product direction requires strong tenant separation suitable for subscription hosting | Future control-plane work will provision one database per tenant instead of shared-table tenancy |
| 2026-03-28 | Expand the hosted bridge with additional PostgreSQL-backed read endpoints before introducing hosted writes | The frontend still needed planning/calendar and grading settings from legacy full-state behavior, and a broader read surface reduces production dependence on `GET /api/state` with lower risk than jumping straight to writes | Hosted bootstrap now hydrates more domains from dedicated APIs; next backend slice should add writes on top of those repository boundaries |
| 2026-03-28 | Start hosted writes with holidays and plans before wider admin-domain CRUD | These workflows are bounded, already have dedicated hosted reads, and let us prove admin-protected PostgreSQL writes without taking on the whole settings surface at once | Hosted mode now writes those domains through backend APIs; subsequent slices can extend the same pattern to daily breaks and grading configuration |
| 2026-03-28 | Extend the hosted write pattern to daily breaks before grade settings | Daily breaks share the same planning/settings boundary as holidays and plans, so they can reuse the established CRUD pattern with less coordination risk than jumping directly into grading configuration | Hosted planning settings are now more consistently backend-owned, and the next logical write slice is grade types plus grading criteria |
| 2026-03-28 | Preserve the existing grade-type draft/apply UX while moving hosted persistence to the backend | The current UI allows several grade-type edits before a single Apply, so switching to per-row hosted writes would change workflow and increase frontend churn | Hosted mode now bulk-replaces `grade_types` on Apply and saves grading criteria directly, keeping the user experience stable while making persistence backend-owned |
| 2026-03-28 | Treat backend route/service extraction as the next implementation priority after the first hosted settings write surface | Hosted reads and key settings writes now exist, but `server/src/app.js` still carries too much domain and validation logic for long-term maintainability | Delivery now shifts from adding bridge endpoints to extracting calendar/grading/curriculum domain boundaries and isolating legacy full-state behavior |
| 2026-03-28 | Start the backend extraction with calendar/planning routes before grading or curriculum | Calendar/planning already had a complete hosted read/write surface and shared validation rules, making it the least risky first extraction target | `server/src/app.js` now delegates that domain to a dedicated route module, establishing the extraction pattern for later grading and curriculum slices |
| 2026-03-28 | Extract grading routes immediately after calendar/planning to preserve a consistent modularization pattern | Grade settings already had dedicated hosted endpoints and backend validation, so they could move cleanly without introducing new contracts | `server/src/app.js` now delegates grading route registration to a dedicated module, reducing central route sprawl and setting up curriculum as the next extraction target |
| 2026-03-28 | Extract curriculum read routes before adding curriculum writes | Subjects, courses, and enrollments already had stable hosted read contracts, so moving the read boundary first reduces file sprawl without coupling extraction to new persistence behavior | `server/src/app.js` now delegates curriculum read registration to a dedicated module, and the next curriculum slice can focus on backend-owned writes instead of mixed refactor plus feature work |
| 2026-03-28 | Include enrollment schedule-order persistence in the first curriculum write slice | The UI already allows hosted admins to change schedule order, so leaving that action browser-owned while other curriculum writes moved server-side would create an inconsistent trust boundary | Hosted curriculum writes now include subject/course/enrollment CRUD plus enrollment schedule-order updates through the backend |
| 2026-03-28 | Move student create/delete onto the backend before pausing for browser smoke validation | Students are a core admin domain with direct downstream effects on enrollments, plans, attendance, tests, daily breaks, and linked users, so leaving them browser-owned would keep too much hosted admin behavior outside the server trust boundary | Hosted student create/delete now persists through the backend and refreshes dependent hosted domains, making the forthcoming smoke pass more representative of the real production path |
| 2026-03-28 | Move hosted user management onto backend-owned APIs before smoke validation | Users are the admin boundary closest to authentication and password trust, so keeping user creation and password updates in browser-only code would leave a major production risk unresolved | Hosted user create/update/delete now uses backend APIs with server-side password hashing and last-admin protection, making the hosted smoke pass materially closer to the intended production trust model |
| 2026-03-28 | Preserve school-year and quarter plan-sync behavior when moving hosted settings writes to the backend | School year and quarter edits are not isolated settings changes; they also affect annual and quarterly plan date ranges in the current product behavior | Hosted school-year and quarter writes now run through backend transactions that keep current-year state and matching plan dates aligned, reducing the risk of drift between configuration and instructional plans |
| 2026-03-28 | Finish hosted attendance and grade writes before the first broad smoke pass | Attendance and grade records are core instructional workflows, and leaving them partially browser-owned would make the validation pass less representative of the intended production trust boundary | Hosted mode now persists attendance CRUD and grade/test create-update-delete through backend APIs, so the smoke pass can exercise a more complete server-owned admin surface |
| 2026-03-28 | Treat the `APP001` host-key mismatch as a stop condition for remote debugging until it is verified | The hosted smoke pass surfaced a real deployment issue, but the SSH follow-up indicated the remote identity for `192.168.1.200` changed; continuing past that warning without verification would create unnecessary security risk | Local backend refactoring can continue, but remote diagnosis/deployment work should pause until the host key change is confirmed out of band |
| 2026-03-28 | Extract user and student routes before tackling further bridge retirement | With calendar, grading, curriculum, and instructional records already split out, user/student handlers were the largest remaining inline admin surface in `server/src/app.js` | `server/src/app.js` now delegates user/student API registration to a dedicated route module, making the remaining legacy bridge and auth bootstrap paths easier to isolate next |
| 2026-03-28 | Treat the hosted browser smoke pass as passed and keep it as the regression gate for future boundary work | The current deployment now validates login/logout plus user/student/curriculum/settings/attendance/grade CRUD flows end to end, which materially reduces uncertainty around the new backend-owned hosted surface | Delivery can move back to code-structure cleanup, but future major backend slices should still rerun the same hosted smoke pass before being treated as release-ready |
| 2026-03-28 | Extract auth/session routes before changing session middleware behavior | Login/logout/session bootstrap already passed the hosted smoke test, so the safer next move is to separate route registration first and keep the request-auth middleware stable until a later slice | `server/src/app.js` now delegates auth route registration to a dedicated module, reducing central route sprawl without changing the current hosted session behavior |
| 2026-03-28 | Extract shared request middleware and the transitional `api/state` bridge after auth routing | Once the domain and auth routes were modularized, the next highest-value cleanup was the remaining entrypoint-owned plumbing and legacy full-state endpoints | `server/src/app.js` now delegates CORS/auth-context middleware setup and the transitional state bridge, leaving the entrypoint closer to pure composition plus health/error/startup wiring |
| 2026-03-28 | Leave `app.js` as the composition root after extracting infra wiring | Once route modules, shared middleware, transitional state endpoints, health checks, and the generic error handler are split out, the remaining value in `server/src/app.js` is primarily dependency wiring and startup composition | `server/src/app.js` is now close to the intended steady state, so future cleanup should focus on dependency shaping and module boundaries rather than trying to eliminate the composition root itself |
| 2026-03-28 | Replace the hosted default admin password path with a one-time initialization flow | The hosted CRUD/auth surface is now stable enough that the remaining bootstrap-admin shortcut stands out as an operational and security debt item | Delivery now shifts toward a backend-owned setup status plus one-time initialization token flow so hosted deployments can establish their first admin without relying on a permanent default password |
| 2026-03-28 | Implement hosted admin initialization as an additive path before removing the old bootstrap seed | The existing deployment and runbooks still rely on the seeded admin path, so the safer delivery sequence is to add the new setup-token flow first, then validate it on a fresh deployment before cutting over operational defaults | The codebase now supports `GET /api/setup/status`, `POST /api/setup/initialize`, setup-token storage, and a first-run setup UI, while bootstrap-admin retirement remains the next operational validation step |
| 2026-03-28 | Treat the current hosted deployment as a safe partial verification target for setup-flow rollout | Fully resetting the active hosted deployment just to rehearse first-run setup would be unnecessarily destructive, but the live environment can still validate migration success, endpoint availability, UI deployment, and initialized-state guardrails | We verified the deployed setup route/UI on the current initialized deployment and updated the hosted runbook to use the new one-time token flow, while a true fresh/reset end-to-end rehearsal remains the next controlled validation step |
| 2026-03-28 | Retire the hosted bootstrap-admin seed path after isolated end-to-end rehearsal | A temporary-schema rehearsal on `APP001` proved the first-run setup flow from uninitialized state through authenticated admin session without risking the live deployment | The production codebase and runbooks now rely solely on setup tokens plus `POST /api/setup/initialize` for first-run hosted admin establishment |
| 2026-03-28 | Record the hosted auth/session contract as a dedicated backend note | The endpoint list alone was no longer enough to communicate cookie behavior, request auth-context semantics, role filtering, and expected status codes after the hosted boundary expansion | Future backend and frontend changes now have a single reference for session ownership and protected-endpoint behavior |
| 2026-03-28 | Define the control plane around tenant environments and durable provisioning jobs before writing `control-api` code | The repo already had operator/control placeholders, but without a concrete environment model and job lifecycle the next implementation step would be under-specified and likely to drift | The new control-plane schema and workflow notes now establish tenants, environments, releases, jobs, and operator boundaries as the reference model for the first control-plane backend slice |
| 2026-03-28 | Scaffold `control-api` with real migrations and route composition before implementing provisioning mutations | The control-plane notes were ready, and the fastest way to reduce ambiguity for Milestone 5 was to create the actual service layout, table migration, and endpoint grouping that later slices will fill in | The repo now has a concrete `control-api` app skeleton, initial control-plane PostgreSQL schema, operator-auth route group, and first tenant/environment/job endpoints to build on |
| 2026-03-28 | Give operator auth its own dedicated contract note instead of folding it into tenant auth docs | Operator sessions, roles, and cookie scope must stay clearly separate from tenant runtime authentication, and mixing them into the tenant contract would blur an important system boundary | Control-plane implementation now has an explicit operator auth/session reference covering cookie naming, role expectations, public vs protected endpoints, and separation from tenant sessions |
| 2026-03-28 | Start `control-api` mutations with platform-admin-guarded tenant/environment creation and job queueing | The scaffold needed at least one real write path to prove the control-plane shape, but jumping straight into host-level provisioning would couple this slice to infrastructure execution too early | The control plane now enforces `platform_admin` for core mutations and records tenant/environment/job intent in PostgreSQL before later slices automate the actual runtime side effects |
| 2026-03-28 | Use a one-time public operator bootstrap endpoint for the first control-plane admin | The new control-plane service needed a deterministic first-operator path similar to the tenant app’s setup flow, and manual SQL-only bootstrapping would slow validation and blur the expected operational contract | `GET /api/operator/setup/status` and `POST /api/operator/setup/bootstrap` now establish the first `platform_admin` once, after which normal operator auth/session flow takes over |
| 2026-03-28 | Validate the new control-plane slice on `APP001` using an isolated schema and temporary port before treating it as stable | The control-plane service is still early and should not be exercised first against any persistent or production-facing runtime state | A non-production rehearsal proved operator bootstrap, session bootstrap, tenant creation, environment creation, and provisioning job queueing end to end while also surfacing and resolving the bootstrap locking bug safely |
| 2026-03-28 | Deploy the control-plane slice on a persistent non-production path under `/control/` and `/control-api/` before any broader rollout | The operator UI and control API needed a stable hosted path for manual validation, but they are not ready to share the tenant app’s production runtime or data boundary | `APP001` now hosts a dedicated control-api service on port `3100` backed by schema `hsm_control_staging`, while `WEB001` exposes the operator UI and proxy path without affecting tenant-app production data |
| 2026-03-28 | Treat the staged `/control/` and `/control-api/` smoke pass as the gate for moving operator-console work from bootstrap basics to richer workflow UX | The public staging deployment now proves login, tenant mutation, environment creation, job queueing, detail reads, and logout through the real proxy path, so the next best investment is usability and execution visibility rather than more bootstrap plumbing | Milestone 5 can now shift toward environment/job history, richer status presentation, and eventually real provisioning execution behind the queued control-plane jobs |
| 2026-03-28 | Add richer operator-console status and history views on top of the staged control-plane shell before real provisioning execution | The staged smoke pass proved the basic control-plane routes, and the next bottleneck for safe operator work was visibility into environment state transitions and queued-job context rather than more bootstrap plumbing | The deployed `/control/` UI now surfaces summary metrics, status-colored badges, environment action history, related jobs, and job lifecycle detail before real host-level execution is wired in |
| 2026-03-28 | Implement tenant runtime resolution from existing domain and environment records before inventing new control-plane tables | The control-plane schema already had enough data to answer hostname-to-environment lookup and database-routing needs, so adding more tables first would increase model drift without unlocking more capability | `control-api` now resolves runtime identity from `tenant_domains` plus `tenant_environments`, with a public host-resolution route and an internal routing route for trusted callers |
| 2026-03-28 | Execute the first queued provisioning jobs inside `control-api` with explicit claim/run/succeed/fail transitions and event logs before moving to host-level automation | The platform needed real durable execution semantics and observable job history now, but jumping straight to SSH/database side effects would have expanded the blast radius too early | The staged control plane now processes `provision_environment` and `issue_setup_token` jobs to `succeeded`, records ordered events, updates environment state, and registers release/setup-token metadata that later infrastructure automation can replace step by step |
| 2026-03-28 | Use the tenant server scripts on `APP001` as the first real provisioning automation path before introducing host-level deployment orchestration | The control-plane service already runs beside the tenant `server/` code, so the fastest safe move from metadata-only jobs to real side effects was to reuse the tenant migration and setup-token scripts against target schemas | Provisioning jobs now create target schemas, run tenant PostgreSQL migrations, and write runtime/setup-token artifacts on `APP001`, while larger host/web deployment automation remains a later slice |
| 2026-03-28 | Propagate environment initialization from tenant runtime status checks before designing a more complex callback system | The tenant runtime already exposes `GET /api/setup/status`, so the control plane can confirm first-run completion immediately without adding a new tenant callback contract first | Control-plane environments can now move from `token_issued` to `initialized` through manual and background setup-state synchronization, while a more secure internal runtime-auth path remains the next hardening step |
| 2026-03-29 | Do not force `APP001` to bypass SSH host verification in order to reach `WEB001` for deployment automation | The next host/web deployment step depends on cross-host trust, and the current `APP001 -> WEB001` path failed at host-key verification; bypassing that in automation would trade away exactly the safety we are trying to build | Real cross-host app/web deployment remains gated on establishing trusted host access, while the worker continues to perform only the safe local automation steps on `APP001` |
| 2026-03-29 | Switch tenant setup-state synchronization from the public setup-status endpoint to a shared-key protected internal route | The control plane needed a safer staging path immediately, and the tenant runtime could expose an internal-only endpoint faster than designing a full callback or mutual-auth model in the same slice | Setup reconciliation now uses `GET /api/internal/setup/status` with `x-control-plane-key`, and the next hardening step is a stronger service-to-service auth model |
