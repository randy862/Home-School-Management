# 2026-05-19

## Class Bulk Enrollment And Schedule QA

- Added bulk student enrollment to Class configuration.
- Added Students multi-select on the Class form with all-active-students support.
- Editing a class preselects enrolled students and the Class table shows enrolled student names under the enrollment count.
- Class roster saves now move a student cleanly from same-course flexible enrollment or another same-course class into the selected class.
- Preserved existing instruction plans when moving from flexible course enrollment into class enrollment.
- Polished the Students and Quarters dropdowns so their checkboxes render as compact, left-aligned checklist rows.

## School Day Schedule Reflow

- Improved School Day placement so flexible/non-class course work reflows around fixed classes.
- Added look-ahead behavior that fills usable windows before later fixed classes with a shorter flexible course when the next ordered course is too long.
- Fixed the anchor ordering regression where a fixed class starting at the current cursor could be skipped and an overlapping Flex Block could be generated.
- Validated the behavior against Dexter Mitchell's live School Day workflow during real usage QA.

## Fixed-Class Conflict Protection

- Added frontend class-time conflict detection by student, overlapping weekdays, effective quarter scope, and class time window.
- The Class form now warns in the Students dropdown when a selected student has a conflicting fixed class.
- Class save is blocked with a clear conflict message before roster changes are applied.
- Added backend validation so section-enrollment create/update and course-section schedule edits reject conflicting fixed classes.
- Backend conflict response uses `409` and direct API paths now have the same protection as the UI.

## Production Deployment

- APP001 backend validation deployed and `hsm-api` restarted healthy.
- WEB001 tenant app deployed with:
  - `app.js?v=202605191515`
  - `styles.css?v=202605191530`
- Rollback snapshots:
  - `/home/debian/rollback/hsm/class-conflict-202605191515/`
  - `/var/www/home-school-management/rollback/web-class-bulk-enroll-202605190845.tgz`
  - `/var/www/home-school-management/rollback/web-school-day-gap-fill-anchor-fix-202605191445.tgz`
  - `/var/www/home-school-management/rollback/web-class-conflict-202605191515.tgz`
  - `/var/www/home-school-management/rollback/web-class-quarters-polish-202605191530.tgz`

## Validation

- `node --check web/app.js` passed.
- `node --check server/src/services/curriculum-service.js` passed.
- `node --check server/src/repositories/postgres/curriculum-repository.js` passed.
- Backend conflict and non-conflict service behavior checks passed with fake repository data.
- APP001 `/health` returned `{"ok":true}` after restart.
- WEB001 public HTML served the expected asset versions.
- Full hosted release gate passed for `https://mitchell.navigrader.com`.

## School Day Bulk Status Actions

- Added `Complete Open` and `Excuse Open` actions to the School Day Daily Schedule header.
- Bulk status actions target the currently filtered scheduled/open instruction queue, including dashboard Open Classes deep links.
- Actions respect active date, student, subject, course, status, and quick filters before persisting instruction actual status records.
- Added confirmation copy showing the number of open classes and affected student count before bulk updates.
- Buttons disable when no open classes match the current queue or while a bulk update is running.

## Bulk Status Deployment

- WEB001 tenant app deployed with:
  - `app.js?v=202605191700`
  - `styles.css?v=202605191700`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-school-day-bulk-status-202605191700.tgz`

## Bulk Status Validation

- `node --check web/app.js` passed.
- WEB001 public HTML serves the expected `202605191700` asset versions.
- Public hosted `/`, `/terms`, and `/privacy` returned HTTP 200.
- Hosted release gate was attempted from Codex but could not run because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.

## School Day Bulk Actions Polish

- Moved `Complete Open` and `Excuse Open` into a quieter `Bulk Actions` disclosure in the Daily Schedule header.
- Removed the always-visible green/yellow bulk buttons so mass actions remain available without competing with daily record keeping.
- The disclosure shows the current open-class count and the individual bulk action buttons remain disabled when no open rows match the queue.
- The confirmation dialog remains in place as the final guardrail before status changes are applied.

## Bulk Actions Polish Deployment

- WEB001 tenant app deployed with:
  - `app.js?v=202605191730`
  - `styles.css?v=202605191730`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-school-day-bulk-actions-subtle-202605191730.tgz`
- `node --check web/app.js` passed.
- WEB001 and public hosted HTML both reference the expected `202605191730` asset versions.

## Recommended Quarter Generation

- Added a recommendation engine that divides a School Year's instructional days into four balanced quarters.
- Instructional day calculations use the existing Monday-Friday school-day model and exclude configured Holidays and Breaks.
- New School Years now create recommended Q1-Q4 ranges automatically when enough instructional days exist.
- The Quarters tab now previews recommended dates, school-day counts, and lets an administrator fill or save the recommendation.
- Holiday/Break additions, updates, and removals rebalance saved quarters only when those quarters still match the prior recommendation.
- Manually edited quarter dates are left alone and can be replaced explicitly from the Quarters tab.

## Quarter Recommendation Deployment

- WEB001 tenant app deployed with:
  - `app.js?v=202605191815`
  - `styles.css?v=202605191815`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-quarter-recommendations-202605191815.tgz`
- `node --check web/app.js` passed.
- WEB001 and public hosted HTML both reference the expected `202605191815` asset versions.
- Public hosted `/`, `/terms`, and `/privacy` returned HTTP 200.
- Hosted release gate was attempted from Codex but could not run because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.

## Quarter Recommendation Disclosure Polish

- Converted the Recommended Quarters panel into a collapsible disclosure on the Quarters tab.
- The collapsed row keeps the recommendation summary visible while hiding the date cards and action buttons.
- Choosing `Use Recommended Dates` opens the disclosure so the parent can immediately review the filled recommendation.
- WEB001 tenant app deployed with:
  - `app.js?v=202605191830`
  - `styles.css?v=202605191830`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-quarter-recommendations-disclosure-202605191830.tgz`
- `node --check web/app.js` passed.
- WEB001 and public hosted HTML both reference the expected `202605191830` asset versions.
- Public hosted `/`, `/terms`, and `/privacy` returned HTTP 200.
- Hosted release gate was attempted from Codex but could not run because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.

## Class Conflict Cleanup Fix

- Fixed hosted class edits so unchecked students are removed before the backend validates the updated class schedule.
- This allows administrators to clean up already-conflicting class rosters instead of being blocked by the old conflicting enrollment state.
- Remaining selected students are still protected by frontend and backend fixed-class conflict validation.
- If a post-removal hosted update fails, the app refreshes class and section-enrollment state to avoid stale roster data.
- WEB001 tenant app deployed with:
  - `app.js?v=202605191900`
  - `styles.css?v=202605191900`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-class-conflict-removal-fix-202605191900.tgz`
- `node --check web/app.js` passed.
- WEB001 and public hosted HTML both reference the expected `202605191900` asset versions.
- Public hosted `/`, `/terms`, and `/privacy` returned HTTP 200.
- Hosted release gate was attempted from Codex but could not run because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.
