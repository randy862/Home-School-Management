# Journal - 2026-05-24

## Performance Baseline Instrumentation

- Added opt-in tenant app performance diagnostics enabled by `?perf=1` and disabled again with `?perf=0`.
- Diagnostics record API fetch timing, status, payload bytes, row counts, total hydration timing, per-hydration task timing, Dashboard render timing, and key Dashboard builder timings to `window.__navigraderPerfMetrics`.
- Added `scripts/Measure-HostedPerformance.ps1` to capture hosted endpoint timing, payload size, and row-count baselines from PowerShell without storing credentials in the repo.
- Bumped tenant app to `app.js?v=202605242103`.
- Deployed `web/index.html` and `web/app.js` to the served WEB001 web root.
- Rollback: `/var/www/home-school-management/rollback/web-performance-instrumentation-served-202605242103.tgz`.
- Validation:
  - `node --check web/app.js`
  - PowerShell parser check for `scripts/Measure-HostedPerformance.ps1`
  - `git diff --check`
  - WEB001 local root references `app.js?v=202605242103`.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605242103`.
  - Served tenant app JS contains `hsm_perf_diagnostics`, `hydrate.total`, and `dashboard.render`.
  - Public `https://mitchell.navigrader.com/health`, `/terms`, and `/privacy` returned HTTP 200.
- Full hosted release gate was not run from this shell because hosted smoke credentials were not present.

## Dashboard Monthly-Series Cache

- Reviewed follow-up Mitchell browser metrics after the instructional-hours cache deploy.
- The worst Dashboard render dropped from ~7.5 seconds to ~754 ms, with the first `dashboard.buildInstructionalHoursSnapshot` down to ~289 ms and reused instructional-hours metrics near-zero.
- Added a cached compliance monthly-series builder keyed by school year, current day, selected/visible students, chart filters, quarter dates, and record-data versions.
- Changed compliance monthly-series calculations to reuse cached daily scheduled blocks instead of bypassing the Dashboard scheduler cache.
- Refactored the Instruction Hours Trend chart to use the shared monthly-series builder rather than walking the school year and invoking schedule calculations on its own.
- Added metrics for `dashboard.buildComplianceMonthlySeries`, `dashboard.renderComplianceHoursMonthlyChart`, and `dashboard.renderInstructionHoursTrending`.
- Bumped tenant app to `app.js?v=202605242212`.
- Deployed `web/index.html` and `web/app.js` to the served WEB001 web root.
- Rollback: `/var/www/home-school-management/rollback/web-dashboard-monthly-series-cache-202605242212.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check`
  - WEB001 local root references `app.js?v=202605242212`.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605242212`.
  - Served tenant app JS contains `complianceMonthlySeriesCache`, `dashboard.buildComplianceMonthlySeries`, and `dashboard.renderInstructionHoursTrending`.
  - Public `https://mitchell.navigrader.com/health` returned HTTP 200.
  - Full hosted release gate passed from the user's PowerShell session, including APP001 local health, public health, public legal pages, and hosted tenant smoke.

## Dashboard Instructional-Hours Cache

- Reviewed Mitchell tenant browser metrics from `window.__navigraderPerfMetrics`.
- The primary lag was Dashboard rendering, especially repeated `dashboard.buildInstructionalHoursSnapshot` calls taking up to ~3.3 seconds and full `dashboard.render` calls up to ~7.5 seconds.
- Added indexed instruction-actual and flex-block lookups so schedule calculations no longer repeatedly scan thousands of records by student/course/date.
- Added cached daily scheduled blocks and cached instructional-hours snapshots keyed by school year, reference date, student set, instructor filter, and record-data versions.
- Bumped tenant app to `app.js?v=202605242149`.
- Deployed `web/index.html` and `web/app.js` to the served WEB001 web root.
- Rollback: `/var/www/home-school-management/rollback/web-dashboard-instruction-hours-cache-202605242149.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check`
  - WEB001 local root references `app.js?v=202605242149`.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605242149`.
  - Served tenant app JS contains `instructionalHoursSnapshotCache`, `dashboardDailyBlocksCache`, and `instructionActualsByKey`.
  - Public `https://mitchell.navigrader.com/health` returned HTTP 200.
