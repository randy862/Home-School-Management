# Journal - 2026-05-20

## Curriculum Editor Scroll Fix

- Added a shared `scrollManagementEditorIntoView` helper for the Curriculum Courses/Classes editor forms.
- Course Edit/Create now opens the top course editor, scrolls it into view, and focuses Course Name.
- Class Edit/Create now opens the top class editor, scrolls it into view, and focuses the Course selector.
- This fixes the confusing long-list behavior where clicking Edit loaded the editor off-screen and looked like nothing happened.

## Curriculum Editor Scroll Deployment

- WEB001 tenant app deployed with:
  - `app.js?v=202605200905`
  - `styles.css?v=202605191900`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-curriculum-editor-scroll-202605200905.tgz`
- `node --check web/app.js` passed.
- Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605200905`.
- Served tenant app JS contains the editor-scroll fix.
- Hosted release gate was not run from Codex because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.

## Student Schedule Edit Cancel Fix

- Renamed row-level Current Schedule actions from `Edit` to `Edit Schedule` to make the schedule-wide mode explicit.
- In edit mode, row actions now show `Editing Schedule` and `Remove`.
- The action row now shows `Save Schedule Changes` plus `Cancel Changes`; cancel restores the original draft without leaving the student page.

## Student Schedule Edit Cancel Deployment

- WEB001 tenant app deployed with:
  - `app.js?v=202605200916`
  - `styles.css?v=202605191900`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-student-schedule-edit-cancel-202605200916.tgz`
- `node --check web/app.js` passed.
- Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605200916`.
- Served tenant app JS contains the student schedule edit/cancel fix.
- Hosted release gate was not run from Codex because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.

## Help Center Content Slice

- Converted the topbar Help icon into a functional authenticated Help Center entry point.
- Converted the sidebar `Need Help?` card into a Quick Start Help Center entry point.
- Added parent-friendly task articles for Quick Start, Dashboard, Students, Courses, Classes, Schedule, School Day, Grades, Attendance, Reports, Account/Billing, and Troubleshooting.
- Help now opens contextually based on the current app tab and includes related-page shortcuts.

## Help Center Deployment

- WEB001 tenant app deployed with:
  - `app.js?v=202605201454`
  - `styles.css?v=202605201454`
- Rollback snapshot:
  - `/var/www/home-school-management/rollback/web-help-center-202605201454.tgz`
- `node --check web/app.js` passed.
- Public `mitchell` and `smoketest` tenant roots returned HTTP 200 and reference the new app/style asset versions.
- Served tenant app JS contains `HELP_ARTICLES` and `renderHelpCenterSurface`.
- Served tenant CSS contains `help-center-modal-body`.
- Hosted release gate was not run from Codex because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.

## Export Artifact Cleanup Hardening

- Added a control-plane maintenance service for paid data export retention.
- The existing provisioning worker now runs due maintenance without blocking job processing if cleanup fails.
- Expired `ready` cancellation/data-export requests are marked `expired` once `artifact_expires_at <= NOW()`.
- Expired ZIP artifacts are deleted only after resolving inside `CONTROL_DATA_EXPORT_DIR`.
- Missing files are tolerated and unsafe artifact paths are skipped with a warning.
- Added `CONTROL_MAINTENANCE_*` and `CONTROL_DATA_EXPORT_DIR` examples to `.env.example`.
- Added migration `012_export_cleanup_indexes.sql` for efficient expiration lookup.

## Export Cleanup Deployment

- APP001 rollback snapshot:
  - `/home/debian/rollback/hsm/control-api-export-cleanup-202605201610/app001/control-api.tgz`
- Deployed updated control-api files to APP001.
- Applied control migrations through `012_export_cleanup_indexes.sql`.
- Corrected deployed migration drift by syncing missing `009_subscription_package_branding.sql` to APP001 before rerunning migrations.
- Restarted `hsm-control-api.service`; service is active.
- `node --check` passed locally and on APP001 for changed control-api files.
- Focused cleanup simulation passed for deleted, missing, and unsafe-path artifact cases.
- Maintenance retry timing simulation passed, confirming failed cleanup attempts retry on the next worker tick.
- APP001 local control health returned `{"ok":true}`.
- Public `https://mitchell.navigrader.com/control-api/health` returned `{"ok":true}`.
- Hosted release gate was not run from Codex because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` are not available in this process.

## Independent Learning and School Day Polish Closeout

- Added protected `Independent Learning` system instructor support for self-led homeschool courses/classes.
- Course instructor, Class instructor override, School Day row instructor edit, reports, dashboard gauges, grade filters, and instructor filters now include `Independent Learning`.
- Added tenant migration `031_independent_learning_instructor.sql` to seed the protected instructor and add `course_sections.instructor_id`.
- Class-level instructor override now persists, and School Day class rows default to the class instructor before falling back to the course instructor.
- Tightened the Classes Weekdays field to content width.
- Replaced the Curriculum sidebar icon with a clean outline SVG and cache-busted it, fixing the active-state white square.
- Fixed School Day row edit draft loss: unsaved start time, instructor, and minutes now survive status/grade re-renders until Save/Cancel/Reset.

## Independent Learning and School Day Polish Deployment

- APP001 rollback snapshot:
  - `/home/debian/rollback/hsm/independent-learning-instructor-202605202030/app001/server.tgz`
- WEB001 rollback snapshots:
  - `/var/www/home-school-management/rollback/web-class-weekdays-compact-202605202115.tgz`
  - `/var/www/home-school-management/rollback/web-curriculum-icon-fix-202605202130.tgz`
  - `/var/www/home-school-management/rollback/web-school-day-edit-draft-preserve-202605202145.tgz`
- Deployed tenant app assets:
  - `app.js?v=202605202145`
  - `styles.css?v=202605202115`
  - `book-open.svg?v=202605202130`
- `node --check web/app.js` passed.
- APP001 `hsm-api.service` restarted active after migration; local `/health` returned `{"ok":true}`.
- WEB001 returned HTTP 200 after each web deployment.
- Public `mitchell` and `smoketest` tenant roots reference the new app/style/icon asset versions.
- Served tenant app JS contains `preserveActiveSchoolDayInstructionEditDraft`.
- Served tenant CSS contains the compact Class weekdays selector.
- Served Curriculum icon SVG no longer contains a white background rectangle.
- Full hosted release gate passed for `https://mitchell.navigrader.com`.
