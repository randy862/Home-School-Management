# Journal - 2026-05-22

## Dashboard Attendance Open Navigation

- Routed the Dashboard `Attendance Open` gauge directly to School Day > Attendance.
- Kept the other dashboard open-item gauges pointed at Daily Schedule.
- Bumped the tenant app cache version to `app.js?v=202605221010`.
- Deployed `web/index.html` and `web/app.js` to WEB001.
- Rollback: `/var/www/home-school-management/rollback/web-dashboard-attendance-tab-link-202605221010.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check -- web/app.js web/index.html`
  - WEB001 root returned HTTP 200.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605221010`.
  - Served tenant app JS contains the Attendance Open gauge tab target.

## Help Center Quick Start Replacement

- Replaced the in-app Quick Start article with the provided detailed setup guidance from `navigrader_help_center_quick_start.md`.
- Added rich Help article rendering support for the longer Quick Start content.
- Saved the prior in-app Quick Start article at `web/help/quick-start-previous-20260522.md`.
- Bumped tenant app assets to `app.js?v=202605221130` and `styles.css?v=202605221130`.
- Deployed `web/index.html`, `web/app.js`, and `web/styles.css` to WEB001.
- Rollback: `/var/www/home-school-management/rollback/web-quick-start-guidance-202605221130.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check -- web/app.js web/styles.css web/index.html web/help/quick-start-previous-20260522.md`
  - WEB001 root returned HTTP 200.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605221130` and `styles.css?v=202605221130`.
  - Served tenant app JS contains the new Quick Start guidance.
  - Served tenant CSS contains rich Help article styles.

## Help Center Pop-Out Window

- Added an `Open in Window` action to the Help Center modal.
- The pop-out is a separate browser window with the same Help topics and article content, so parents can keep guidance visible while using the main app.
- Pop-out topic changes stay inside the reference window.
- `Open Related Page` from the pop-out activates the related page in the main app and leaves the reference window open.
- Bumped tenant app assets to `app.js?v=202605221245` and `styles.css?v=202605221245`.
- Deployed `web/index.html`, `web/app.js`, and `web/styles.css` to WEB001.
- Rollback: `/var/www/home-school-management/rollback/web-help-center-popout-202605221245.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check -- web/app.js web/styles.css web/index.html`
  - WEB001 root returned HTTP 200.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605221245` and `styles.css?v=202605221245`.
  - Served tenant app JS contains the Help Center pop-out code.
  - Served tenant CSS contains the Help Center pop-out button style.
