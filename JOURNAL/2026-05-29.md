# 2026-05-29

## Course Minutes/Day UI

Context: Curriculum Courses were configured in hours/day while School Day displayed planned instruction in minutes.

Current state:

- Changed the Course form label/input to `Minutes / Day` with 15-minute minimum and 5-minute steps.
- Existing courses load as minutes by converting the internal `hoursPerDay` value.
- Saves convert entered minutes back to the existing internal `hoursPerDay` field, avoiding a database/API migration.
- Updated Course help text and frontend validation copy to minutes/day.
- Updated backend curriculum validation wording to neutral daily instructional time.
- Class metadata now displays inherited course minutes/day beside instructor and fixed start time.
- Bumped the tenant app cache key to `app.js?v=202605291000`.
- Deployed the web update to WEB001 and the validation wording update to APP001.
- AWS migration carry-forward: deploy AWS APP001/WEB001 from the latest branch so Course minutes/day UI is included before go-live validation.

Validation:

- `node --check web/app.js` passed.
- `node --check server/src/services/curriculum-service.js` passed.
- `git diff --check` passed with only LF/CRLF warnings.
- APP001 `hsm-api.service` restarted active.
- Apache configtest returned `Syntax OK`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- APP001 local `http://127.0.0.1:3000/health` returned HTTP 200.
- Public root references `styles.css?v=202605281100` and `app.js?v=202605291000`.
- Served app script contains `Minutes/Day`, conversion helpers, and Class inherited minutes/day metadata.
- WEB001/APP001 deployed SHA-256 hashes matched local changed files.

Rollback:

- Rollback root: `/home/debian/rollback/hsm/course-minutes-day-202605291000/`

## Attendance-Driven Class Excusals

Context: If a student is absent for a school day, their open scheduled classes for that day should not remain in the completion queue.

Current state:

- Marking a student `Absent` now automatically marks that student's scheduled/open instruction rows as `Excused` for that date.
- Existing `Completed` or already `Excused` instruction rows are left unchanged.
- Changing attendance back to `Present` does not automatically undo existing class statuses.
- School Day attendance save messages include the number of scheduled classes excused.
- Bumped the tenant app cache key to `app.js?v=202605291030`.
- Deployed the web update to WEB001.
- AWS migration carry-forward: deploy AWS WEB001 from the latest branch so attendance-driven excusals are included before go-live validation.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed with only LF/CRLF warnings.
- Apache configtest returned `Syntax OK`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `styles.css?v=202605281100` and `app.js?v=202605291030`.
- Served app script contains attendance-driven auto-excusal helpers.
- WEB001 deployed SHA-256 hashes matched local `web/index.html` and `web/app.js`.

Rollback:

- WEB001 rollback: `/home/debian/rollback/hsm/attendance-absent-auto-excuse-202605291030/web001/web.tgz`

## AWS Stripe Subscription Rehearsal

Context: AWS needed validation for the commercial subscription path before production DNS cutover: Stripe checkout, webhook processing, tenant provisioning, setup email delivery, first-admin setup, and login.

Current state:

- Created Stripe test products/prices for `starter_monthly`, `growth_monthly`, and `large_monthly`.
- Created Stripe test webhook endpoint for `https://aws-validation.navigrader.com/control-api/api/public/billing/webhook`.
- Added Stripe test secret/publishable/webhook values to AWS APP001 control API runtime env; secrets remain outside the repo.
- Updated AWS `hsm_control_staging.commercial_plans` with the Stripe test product/price IDs.
- Enabled AWS control rehearsal flags: `CONTROL_WORKER_ENABLED=true`, `CONTROL_SETUP_SYNC_ENABLED=true`, and `CONTROL_DEPLOYMENT_ENABLED=false`.
- Restored temporary MAINT001 egress long enough for Stripe/Postmark validation.
- Public checkout now creates real Stripe test checkout sessions.
- Completed a browser Stripe checkout that provisioned tenant `aws1`.
- Added GoDaddy A record `aws1 -> 18.188.35.157`.
- Expanded AWS TLS certificate to include `aws1.navigrader.com`.
- Setup email was delivered for `aws1`; first admin setup succeeded; login succeeded.
- AWS control-plane state now shows tenant `aws1` active, environment ready, setup initialized, provisioning ready, active subscription, and sent setup email.
- Removed the temporary AWS route/security-group/source-destination-check NAT path after validation.
- Removed MAINT001 Linux NAT: IP forwarding is off and iptables MASQUERADE is removed.
- APP001 no longer has temporary outbound egress to Stripe/Postmark/checkip, as expected.
- Laptop DNS now resolves `aws1.navigrader.com` to `18.188.35.157`.
- APP001's temporary `/etc/hosts` override for `aws1.navigrader.com` was later removed after DNS caught up; APP001 now resolves `aws1.navigrader.com` to `18.188.35.157` without help.
- Go-live egress decision is documented: use a managed NAT Gateway for APP001 Stripe/Postmark egress at production cutover, not MAINT001 NAT.
- Production DNS cutover rehearsal and rollback steps are documented in `RUNBOOKS/production-cutover.md`.
- `aws1` and related Stripe test records are intentionally kept for now as rehearsal evidence.

Validation:

- APP001 egress through temporary NAT reached Stripe, Postmark, and `checkip.amazonaws.com` during rehearsal.
- After cleanup, APP001 Stripe/Postmark/checkip calls time out again.
- MAINT001 still has direct internet after NAT cleanup.
- AWS HTTPS setup status for `aws1.navigrader.com` returns `initialized:true`.
- Browser flow reached the setup link, created the first admin, and logged in successfully.
- `git diff --check` passed with only LF/CRLF warnings.

Rollback and cleanup pointers:

- AWS Stripe env rollback: `/home/admin/rollback/hsm/aws-stripe-readiness-20260530012023/app001/`
- AWS commercial plan rollback: `/home/admin/rollback/hsm/aws-stripe-readiness-20260530012105/sql001/`
- AWS worker rehearsal rollback: `/home/admin/rollback/hsm/aws-stripe-worker-rehearsal-20260530015453/app001/`
- Temporary NAT rollback snapshot: `/home/admin/rollback/hsm/temp-nat-maint001-20260530012439/`
- AWS1 TLS rollback: `/home/admin/rollback/hsm/aws1-validation-tls-20260530020648/web001/`
- AWS1 APP001 hosts override rollback: `/home/admin/rollback/hsm/aws1-app-hosts-override-20260530021432/app001/`
- AWS1 final APP001 hosts cleanup rollback: `/home/admin/rollback/hsm/aws1-app-hosts-final-cleanup-20260531010706/app001/`
- AWS1 tenant status rollback: `/home/admin/rollback/hsm/aws1-tenant-active-20260530021544/sql001/`

Next pickup:

- Continue production DNS planning and select the first live tenant cutover target.
- Schedule the go-live rehearsal and rollback decision window.
- At go-live, create the managed NAT Gateway, validate APP001 Stripe/Postmark egress, then run the production cutover checklist.
