# 2026-05-29

## Course Minutes/Day UI

Context: Curriculum Courses were configured in hours/day while School Day displayed planned instruction in minutes.

Current state:

- Changed the Course form label/input to `Minutes / Day` with 15-minute minimum and 5-minute steps.
- Existing courses load as minutes by converting the internal `hoursPerDay` value.
- Saves convert entered minutes back to the existing internal `hoursPerDay` field, avoiding a database/API migration.
- Updated Course help text and frontend validation copy to minutes/day.
- Updated backend curriculum validation wording to neutral daily instructional time.
- Class metadata now displays inherited course minutes/day beside instructor and fixed start time.
- Bumped the tenant app cache key to `app.js?v=202605291000`.
- Deployed the web update to WEB001 and the validation wording update to APP001.
- AWS migration carry-forward: deploy AWS APP001/WEB001 from the latest branch so Course minutes/day UI is included before go-live validation.

Validation:

- `node --check web/app.js` passed.
- `node --check server/src/services/curriculum-service.js` passed.
- `git diff --check` passed with only LF/CRLF warnings.
- APP001 `hsm-api.service` restarted active.
- Apache configtest returned `Syntax OK`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- APP001 local `http://127.0.0.1:3000/health` returned HTTP 200.
- Public root references `styles.css?v=202605281100` and `app.js?v=202605291000`.
- Served app script contains `Minutes/Day`, conversion helpers, and Class inherited minutes/day metadata.
- WEB001/APP001 deployed SHA-256 hashes matched local changed files.

Rollback:

- Rollback root: `/home/debian/rollback/hsm/course-minutes-day-202605291000/`

## Attendance-Driven Class Excusals

Context: If a student is absent for a school day, their open scheduled classes for that day should not remain in the completion queue.

Current state:

- Marking a student `Absent` now automatically marks that student's scheduled/open instruction rows as `Excused` for that date.
- Existing `Completed` or already `Excused` instruction rows are left unchanged.
- Changing attendance back to `Present` does not automatically undo existing class statuses.
- School Day attendance save messages include the number of scheduled classes excused.
- Bumped the tenant app cache key to `app.js?v=202605291030`.
- Deployed the web update to WEB001.
- AWS migration carry-forward: deploy AWS WEB001 from the latest branch so attendance-driven excusals are included before go-live validation.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed with only LF/CRLF warnings.
- Apache configtest returned `Syntax OK`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `styles.css?v=202605281100` and `app.js?v=202605291030`.
- Served app script contains attendance-driven auto-excusal helpers.
- WEB001 deployed SHA-256 hashes matched local `web/index.html` and `web/app.js`.

Rollback:

- WEB001 rollback: `/home/debian/rollback/hsm/attendance-absent-auto-excuse-202605291030/web001/web.tgz`
