
## Profile Photo Tenant Schema Fix

- Fixed profile photo migration to add `profile_photo_data_url` to every PostgreSQL schema with a `users` table, not only the active search-path schema.
- Applied the corrected migration SQL on APP001.
- Verified all tenant schemas, including `tenant_mitchell_family`, now have the column.
- Validated APP001 service health and public `mitchell.navigrader.com/health`.

## Alerts Slice 1

- Added modern preview alert bell behavior with active alert count and dropdown panel.
- Added calculated alert candidates for open School Day attendance/completion/grade items, instruction pace, grade risk, and attendance risk.
- Added click-through actions to School Day, Grades Search, Attendance Search, and Dashboard.
- Deployed preview assets to WEB001 with cache key `202605060735`.
- Validation: `git diff --check`, local Node syntax check through `C:\Program Files\nodejs\node.exe --check web/app.js`, preview cache-key check, and hosted health check.

## Alerts Panel Density Cleanup

- Reworked alert result rows into stacked cards to remove crowding.
- Removed the horizontal scroll path from the alerts dropdown while retaining vertical scrolling for long alert lists.
- Deployed preview stylesheet update with cache key `202605060815`.
- Validation: `git diff --check`, preview cache-key check, and hosted health check.

## Alerts Slice 2

- Added an Alerts tab under Administration.
- Added tenant/workspace-config settings for alert type visibility and grade/attendance risk thresholds.
- Wired the alert bell calculations to use configured thresholds instead of hardcoded values.
- Deployed preview assets with cache key `202605060910`.
- Validation: `git diff --check`, local Node syntax check, preview cache-key check, and hosted health check.

## Alerts Settings Persistence Fix

- Fixed hosted workspace config normalization so the `alerts` section is persisted instead of stripped on save.
- Deployed `workspace-config-service.js` to APP001 and restarted `hsm-api.service`.
- Verified alert threshold values round-trip through the hosted workspace config service.
- Restored probe thresholds to defaults after validation.
- Validation: `git diff --check`, local Node syntax checks for changed service and `web/app.js`, APP001 service health.

## Alerts Link And Timing Refinement

- Changed Open Class Completion and Missing Grades alerts to count only classes whose School Day scheduled end time has passed.
- Kept Open Attendance, Missing Grades, and Open Completion alert links pointed to School Day with the matching student and quick filter applied.
- Added Behind Instructional Days alert with a placeholder dashboard action label for a future days dashboard.
- Changed Behind Instructional Hours alerts to open Dashboard > Compliance and expand the matching student's instructional-hours row.
- Deployed preview app update with cache key `202605061040`.
- Validation: `git diff --check`, local Node syntax check, preview cache-key check, and hosted health check.

## Alerts Acknowledgement Foundation

- Added manual Acknowledge actions to alert cards.
- Kept alert open/navigation separate from acknowledgement.
- Added browser-persisted acknowledgement records scoped by host and signed-in user.
- Changed completion and missing-grade alerts to stable per student/course/date instances.
- Kept exact-instance acknowledgements suppressed indefinitely while risk/pace alerts re-alert after 7 days.
- Deployed preview assets with cache key `202605061130`.
- Validation: `git diff --check`, local Node syntax check, preview cache-key check, and hosted health check.

## Alerts Slice 5 Cadence Configuration

- Added configurable risk/pace alert re-alert cadence under Administration > Alerts.
- Replaced the fixed 7-day temporary acknowledgement window with the configured cadence value.
- Persisted `riskAlertCadenceDays` through hosted workspace config normalization.
- Deployed backend workspace config service update to APP001 and restarted `hsm-api.service`.
- Deployed preview app update with cache key `202605061215`.
- Validation: `git diff --check`, local Node syntax checks, backend normalization probe, preview cache-key check, and hosted health check.

## Alerts Cadence Label Cleanup

- Moved the re-alert cadence field into its own Ongoing Risk/Pace Re-alerting section.
- Clarified that cadence applies only to Behind Instructional Days, Behind Instructional Hours, Grade Risk, and Attendance Risk.
- Clarified that open attendance, completion, and missing-grade alerts remain acknowledged for exact instances.
- Deployed preview markup with cache key `202605061245`.
- Validation: `git diff --check`, preview cache-key check, and hosted health check.

## Alert Settings UI Cleanup

- Narrowed alert threshold/cadence number fields so they no longer span the full row.
- Renamed Ongoing Risk/Pace Re-alerting to Alert Persistence.
- Updated Alert Persistence explanatory copy per product direction.
- Renamed cadence field to `Re-alert after number of days:`.
- Deployed preview stylesheet/markup update with cache key `202605061300`.
- Validation: `git diff --check`, preview cache-key check, and hosted health check.

## Alert Refresh After School Day Changes

- Fixed stale alert badge/menu behavior after School Day completion, attendance, and grade changes.
- Alert menu now recalculates after instruction, attendance, or grade rerender helpers run.
- Deployed preview app update with cache key `202605061330`.
- Validation: `git diff --check`, local Node syntax check, preview cache-key check, and hosted health check.

## Alert Dashboard Compliance Student Filter

- Added a Student filter to Dashboard > Compliance > Student Instructional Hours.
- Wired Behind Instructional Hours alerts to open Dashboard > Compliance, apply the matching student filter, and expand that student's instructional hours row.
- Bumped modern preview CSS/JS cache keys and deployed updated preview assets to WEB001.
- Validation: git diff --check; node --check web/app.js.

## Dashboard Compliance Gauge Upgrade

- Upgraded Dashboard > Compliance > Required Instructional Hours from simple cards to a forecast gauge.
- Added required hours, logged hours, expected today, current diff, projected hours, and projected diff metrics.
- Added logged/projected gauge fills with expected-today and required-hour markers for at-a-glance compliance insight.
- Bumped modern preview CSS/JS cache keys and deployed updated preview assets to WEB001.
- Validation: git diff --check; node --check web/app.js; hosted health check.

## Dashboard Compliance Gauge Cleanup

- Added a Student filter to the Required Instructional Hours forecast gauge.
- Aligned the Student Breakdown table with the gauge metric cards: required, logged, expected today, current diff, projected, projected diff, and status.
- Staggered Expected/Required marker labels and tightened gauge header controls to reduce crowding.
- Bumped modern preview CSS/JS cache keys and deployed updated preview assets to WEB001.
- Validation: git diff --check; node --check web/app.js; hosted health check.

## Dashboard Compliance Visual Sampler

- Added a Required Instructional Days ring gauge to Dashboard > Compliance.
- Added real instructional-days compliance metrics: required days, completed days, expected today, projected diff, and status.
- Upgraded Student Instructional Hours rows with student initials, status text, and compact progress bars in instructional-hours cells.
- Bumped modern preview CSS/JS cache keys and deployed updated preview assets to WEB001.
- Validation: git diff --check; node --check web/app.js; hosted health check.

## Dashboard Overview Bullet Gauges

- Converted the Dashboard > Overview Instruction Days and Instruction Hours cards into polished bullet-gauge KPI cards.
- Added live bullet fills, target markers, and compact progress notes for days and hours.
- Bumped modern preview CSS/JS cache keys and deployed updated preview assets to WEB001.
- Validation: git diff --check; node --check web/app.js; hosted health check.
