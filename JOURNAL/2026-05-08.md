# Journal - 2026-05-08

## Modern Preview Dashboard Polish

- Modernized Course Watchlist to share the Student Performance card, heading, icon, table, score pill, grade chip, and status pill treatment.
- Fixed Student Performance grading inconsistency where a displayed `90.0%` could show a `B` because raw values were floored before letter-grade lookup.
- Aligned Student Performance score pill color classification with the same one-decimal rounded value used by display and letter grade.
- Rebuilt Grade Type Volume to match the supplied target design:
  - Large icon title header and subtitle.
  - Icon-styled Student, Quarter, Subject, Instructor, and Grade Type filters.
  - Inner chart panel with `Grade Volume by Type` heading.
  - Bar value labels.
  - Summary metric strip for totals.
  - Working `View as Table` / `View as Chart` toggle.
- Set Grade Type Volume ordering to Assignment, Quiz, Test, Quarter Final.
- Fixed Grade Type Volume issues found during preview validation:
  - April Assignment label was clipped.
  - Y-axis top values overlapped.
  - Quarter filter was too narrow.
  - `View as Table` previously did nothing.
- Published updated `web/index.html`, `web/styles.css`, and `web/app.js` to WEB001 `/var/www/home-school-management/web/modern-preview/`.
- Verified public preview cache keys:
  - `styles.css?v=202605072140`
  - `app.js?v=202605072140`
- Validation run:
  - `node --check web/app.js`
  - `git diff --check`

## Notes For Next Session

- Live tenant app was not replaced.
- Existing untracked `tmp/` and icon files were left untouched.
- Continue preview-only workflow unless live cutover is explicitly approved.

## Alert And Trend Chart Follow-Up

- Confirmed Grade Risk was based on weighted course average, not a one-time grade.
- Renamed Grade Risk to Average Grade Risk.
- Added Single Grade Risk as a separate alert type with its own threshold.
- Added client and APP001 server config normalization for `showSingleGradeRisk` and `singleGradeRiskThresholdPercent`.
- Updated Administration > Alerts labels and persistence text.
- Converted Instruction Hours Per Month and Instruction Days Per Month to match Student Grade Trending:
  - Student, Quarter, Subject, Instructor filters.
  - Line View / Area View toggle.
  - Show Data Points toggle.
  - Trend summary cards.
  - Month-wide hover summary tooltips.
- Added month-wide hover summary tooltips to Student Grade Trending and Instructor Grade Trending.
- Adjusted Student Performance filter accents from blue to neutral slate.
- Published preview cache key `202605081022`.
- Deployed backend config normalizer to APP001 and restarted `hsm-api.service`.
- Validation run:
  - `node --check web/app.js`
  - `node --check server/src/services/workspace-config-service.js`
  - `git diff --check`

## Required Instructional Hours Visual Follow-Up

- Replaced the Required Instructional Hours analytics visual with the modern gauge/card layout.
- Tuned the Current Pace gauge needle, hub, percent text, and supporting metric alignment.
- Tuned the Year-End Projection gauge marker so the Required line sits on the arc and the label sits outside the arc.
- Fixed Progress Over Time so missing future/pre-projection values are not plotted as zero.
- Adjusted the projected line to start from the current/YTD point and continue to year end.
- Vertically aligned the metric rows and alert rows across both top gauge cards.
- Published preview cache key `202605081940`.
- Validation run:
  - `node --check web/app.js`
  - `git diff --check`
