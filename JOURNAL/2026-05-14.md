# 2026-05-14 Development Journal

## School Day Workflow Polish

- Refined the School Day interface after user review to keep attention on the Daily Schedule rows.
- Collapsed the filter section so it no longer pushes the schedule down by default.
- Reworked the schedule command/meta row:
  - `+ Student Summaries` and `+ Side-by-Side` controls moved left
  - row count and work queue label centered
  - date and current-view chips moved right
- Made Daily Schedule / Attendance / Grades mode tabs visually distinct from the Needs Attendance / Needs Completion / Needs Grade filters.
- Reordered next-action priority to guide work as:
  `Needs Attendance`, then `Needs Grade`, then `Resolve Past Due`.
- Aligned inline grade entry action buttons to the right so grade processing stays near the row action that opened it.
- Prevented inline grade action buttons from shrinking into unreadable text on smaller laptop screens.
- Renamed the scheduled-class add control to `Add Classes Without Grades`.

## Dashboard And Execution Gauge Polish

- Completed a responsive Dashboard/Execution gauge pass for laptop-width screens while preserving the wider desktop layout.
- Restored the cleaner original gauge spacing for Open Items Today, Open Classes, Grades at Risk, and Missing Required Subjects.
- Restored the original stacked Class Status distribution values.
- Kept the Dashboard cards compact enough for the current production review flow without changing their meaning.

## Compliance Link Behavior

- Updated Dashboard / Compliance / Required Subjects student-count links to behave like Dashboard / Overview / Missing Required Subjects links.
- Single matching student links open that student's schedule/enrollment workflow directly.
- Multiple matching students route to the Students list filtered to the relevant compliance set.

## Production Promotion

- Deployed the School Day polish to WEB001.
- Deployed the Dashboard/Execution responsive gauge polish to WEB001.
- Latest production code commit is `6f30caf Polish dashboard responsive behavior`.
- Live web assets after the latest deploy:
  - `styles.css?v=202605141800`
  - `app.js?v=202605141815`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-20260514170419.tgz`

## Validation

- `node --check web/app.js`
- `git diff --check` on touched web files before deployment
- WEB001 Apache config syntax OK
- WEB001 Apache service active
- Public health endpoint returned `200`
- Live HTML referenced the expected CSS and JS cache keys

## Notes For Resume

- User liked the latest School Day layout and Dashboard cleanup.
- User accepted the low-resolution 1366x768 Hour-column wrapping as a minor issue.
- User still needs more time for browser review before choosing the next UI polish target.
- Untracked local screenshot/icon scratch assets remain outside the committed work.

## Planning Readiness And Calendar Polish

- Promoted Schedule Blocks readiness review filtering:
  - Schedule Blocks table now shows assigned active-student count.
  - Review Blocks filters to schedule blocks not assigned to any active student.
  - Unassigned blocks are highlighted with an active filter strip.
- Promoted Calendar workflow polish:
  - Calendar filters collapse into a compact drawer.
  - Month/Week/Day use a segmented control instead of the View dropdown.
  - Month/week student cards show every scheduled class with class names and hours.
  - Month/week days now default to scheduled student rows that expand to class details.
  - Active date range appears as a compact header pill.
- Promoted archived-student compliance handling:
  - Archived students are not evaluated for required-subject compliance.
  - Archived rows show a neutral Not Evaluated state instead of Not In Compliance.
  - Archived rows no longer show Fix Enrollment or Student Detail missing-required actions.

## Latest Production Checkpoint

- Latest production code commits:
  - `252c535 Focus schedule block readiness review`
  - `37937e3 Polish calendar workflow`
  - `f8f2758 Add expandable calendar student rows`
  - `c626fe4 Skip archived students in required compliance`
- Latest documentation commits:
  - `d50e6c4 Record schedule block readiness release`
  - `60569e9 Record calendar workflow release`
- Live web assets after latest deploy:
  - `styles.css?v=202605141618`
  - `app.js?v=202605141618`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-202605141618.tgz`

## Validation Addendum

- `node --check web/app.js`
- `git diff --check` on touched web files before deployment
- Web Preview returned `200`
- WEB001 Apache config syntax OK
- WEB001 Apache service active
- Public health endpoint returned `200`
- Live HTML referenced the expected CSS and JS cache keys
- Remote WEB001 hashes matched local `web/index.html`, `web/app.js`, and `web/styles.css`

## Reports And Administration Polish

- Promoted the approved Reports/Admin Web Preview bundle to WEB001.
- Reports now shows a compact active-criteria summary above the form.
- Report Content is now a collapsed drawer with selected-section counts.
- Student Report content ordering now places `Detailed Grades` and `Detailed Attendance` at the end of the list.
- Administration now shows a compact active-section summary.
- Workspace Configuration now summarizes visible School Day controls and Dashboard modules.
- School Day Visibility and Dashboard Visibility settings are now collapsible groups with enabled counts.

## Reports/Admin Production Checkpoint

- Production code commit: `942968d Polish reports and administration workspace`.
- Live web assets:
  - `styles.css?v=202605142214`
  - `app.js?v=202605142214`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-202605142214.tgz`

## Reports/Admin Validation

- `node --check web/app.js`
- `git diff --check -- web/app.js web/index.html web/styles.css`
- Web Preview returned `200`
- WEB001 Apache config syntax OK
- WEB001 Apache service active
- Public health endpoint returned `200`
- Live HTML referenced the expected CSS and JS cache keys
- Remote WEB001 hashes matched local `web/index.html`, `web/app.js`, and `web/styles.css`
- Authenticated hosted smoke was attempted with preview credentials, but production rejected them as invalid.

## Account/Billing Polish Release

- Promoted the approved Account/Billing polish bundle to WEB001.
- Committed the production code bundle as `2fa0b01 Polish account billing surfaces`.
- Account modal now includes a compact plan, subscription, and site status summary.
- Local Web Preview now shows realistic Starter subscription data instead of the prototype-only empty state.
- Subscription details now present pricing, included billable students, current usage, billing period, and upgrade action in a cleaner card.
- Available Upgrades now uses richer plan cards for Growth and Co-op Pro with plan-specific accents and clearer CTAs.
- Hosted smoke and release-gate scripts now support credential environment variables:
  - `HSM_HOSTED_SMOKE_USERNAME`
  - `HSM_HOSTED_SMOKE_PASSWORD`
  - `HSM_CONTROL_SMOKE_USERNAME`
  - `HSM_CONTROL_SMOKE_PASSWORD`
- Hosted deployment runbook now recommends environment variables instead of inline credentials.

## Account/Billing Production Checkpoint

- Live web assets:
  - `styles.css?v=202605142310`
  - `app.js?v=202605142310`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-202605142316.tgz`

## Account/Billing Validation

- `node --check web/app.js`
- PowerShell parser checks for `scripts/Test-HostedSmoke.ps1` and `scripts/Invoke-HostedReleaseGate.ps1`
- `git diff --check` on touched release files
- Public health endpoint returned `{"ok":true}`
- Live HTML referenced the expected CSS and JS cache keys
- Authenticated hosted smoke still needs valid production tenant credentials if required.

## Resume UI Polish Queue

1. Global consistency sweep in Web Preview:
   - Check repeated page patterns for compact headers, status chips, action placement, and responsive table behavior.
   - Keep changes small and production-promote only after preview review.
2. Student Detail deeper polish:
   - Review student schedule, enrollment, attendance, grades, and compliance sections for density and clearer next actions.
   - Preserve the current compliance behavior for archived students.
3. Curriculum / Schedule setup microcopy and density pass:
   - Continue from the Planning / Scheduled Items workstream.
   - Improve course, class, schedule-block, and enrollment setup language so it is clearer what will appear in School Day.
