# Session Journal

Date: 2026-05-05

## Calendar, Administration, And Reports Modernization

- Applied the modern white-card preview style to Calendar filters, month/week/day calendar surfaces, navigation controls, day cards, and calendar tables.
- Applied the modern segmented-tab and card style to Administration workspace configuration, checklist rows, default selects, preview cards, and actions.
- Applied the modern card/form style to Reports criteria, content selection, filters, checklist rows, and generate action.
- Bumped the modern preview stylesheet cache key to `styles.css?v=202605051015`.
- Scope remained frontend styling and preview-only; no behavior or data logic was intentionally changed.

## Grades And Attendance Default Entry Forms

- Changed Attendance so the Enter Attendance form remains visible by default for administrators instead of being hidden until `Enter New Attendance` is clicked.
- Changed Grades so the Enter Grades tab automatically keeps one blank grade-entry row visible when students, subjects, and courses exist.
- Moved `Enter New Attendance` and `Enter New Grade` below the entry fields on the left side.
- Bumped preview cache keys to `styles.css?v=202605051045` and `app.js?v=202605051045`.

## Curriculum Weekday Checkbox Regression Fix

- Fixed inconsistent weekday checkbox sizing in the Curriculum Courses and Classes weekday fieldsets by scoping the weekday labels back to inline layout and forcing uniform checkbox dimensions.
- Bumped the modern preview stylesheet cache key to `styles.css?v=202605051100`.
- Tightened the Curriculum weekday fieldset height so it aligns better with the other controls in the same row, and bumped the stylesheet cache key to `styles.css?v=202605051115`.

## Academic Year Global View Context

- Replaced the static sidebar Academic Year card with a real selector populated from configured school years.
- Wired the selector to the existing school-year state helper so year-aware pages rerender against the selected academic year context.
- Stored the selected academic year in local browser storage as a view preference.
- Kept the sidebar selector separate from the tenant-wide official current-year mutation path; Schedule > School Years remains the admin path for changing that.
- Added the Academic Year, Search, and Alerts roadmap to the app modernization note.
- Bumped preview cache keys to `styles.css?v=202605051245` and `app.js?v=202605051245`.

## Academic Year Scoping Pass

- Added active academic-year date helpers and a default reference-date rule: current year uses today, past years use the school year end, and future years use the school year start.
- Scoped Grades and Attendance default record lists to the active academic year.
- Scoped Dashboard summary/execution metrics to the active academic year reference date and updated date-sensitive copy away from hardcoded "today" wording.
- Made Calendar and School Day default/clamp their reference dates inside the active academic year.
- Updated the modernization plan with the completed scoping pass.
- Bumped preview cache keys to `styles.css?v=202605051345` and `app.js?v=202605051345`.

## SaaS Hero Large-Screen Layout Fix

- Tightened the `saas.html` hero grid so the headline column keeps enough width on large monitors.
- Reduced the maximum hero headline size and allowed the image column to shrink within the grid instead of clipping the "Teach More / Manage Less" text.
- Bumped the SaaS stylesheet cache key to `saas.css?v=202605051430`.

## Profile Photo Slice 2

- Added production-backed account profile photo storage on tenant users.
- Added authenticated `/api/account/profile-photo` update endpoint.
- Moved upload/remove workflow into the View Account modal instead of the account menu.
- Deployed backend changes to APP001 and ran PostgreSQL migration `023_user_profile_photo.sql`.
- Deployed modern preview assets to WEB001 with cache key `202605052030`.
- Validation: `git diff --check`, remote `node --check` for changed JS files, APP001 health, public hosted health, and preview cache-key check.
