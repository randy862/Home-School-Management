# 2026-05-28

## Home Lab Open Items Detail Gauge Hotfix

Context: The Open Items Today overview counted open scheduled items, but the Execution dashboard detail row showed Past Due, Grades, Overrides, Attendance, and Class Status without the matching open scheduled-items card.

Current state:

- Restored the `Schedule Items Open` detail gauge in `web/app.js`.
- The gauge uses `needsCompletionCount` and opens the School Day `needs-completion` quick filter.
- Kept Past Due Schedule Items as its own separate detail gauge.
- Bumped the tenant app cache key to `app.js?v=202605280915`.
- Deployed the fix to home lab WEB001.
- AWS migration carry-forward: deploy AWS WEB001 web assets through `0b66c4f` before go-live validation so this hotfix is included.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `app.js?v=202605280915`.
- Served app script contains `Schedule Items Open` and `Past Due Schedule Items`.
- WEB001 deployed SHA-256 hashes matched local `web/index.html` and `web/app.js`.

Rollback:

- WEB001 rollback: `/home/debian/rollback/hsm/web-open-items-schedule-card-202605280915/web001/web.tgz`

## Grade Search Course/Class/Block Filter

Context: Grade Search still used a single Course dropdown while School Day had the improved grouped Scheduled Item dropdown.

Current state:

- Replaced the Grade Search Course filter with one grouped `Course/Class/Block` dropdown.
- The dropdown lists Classes, Courses, and Schedule Blocks using the same compact option presentation as School Day.
- Grade Search filtering now maps selected Classes and Courses back to course IDs for grade matching.
- Dashboard grade search drill-downs now preselect the matching Course in the new grouped dropdown.
- Bumped tenant cache keys to `app.js?v=202605281030` and `styles.css?v=202605281030`.
- Deployed the change to home lab WEB001.
- AWS migration carry-forward: deploy AWS WEB001 from the latest branch so this Grade Search filter is included before go-live validation.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed with only LF/CRLF warnings.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `styles.css?v=202605281030` and `app.js?v=202605281030`.
- Served app script contains `grades-filter-scheduled-item-checkbox` and `Course/Class/Block`.
- Served app script still contains Attendance date filters, School Day Scheduled Item markers, `Schedule Items Open`, and `Past Due Schedule Items`.
- Served CSS contains Grade Search scheduled item dropdown styling.
- WEB001 deployed SHA-256 hashes matched local `web/index.html`, `web/app.js`, and `web/styles.css`.

Rollback:

- WEB001 rollback: `/home/debian/rollback/hsm/web-grade-search-scheduled-item-filter-202605281030/web001/web.tgz`

## Grade Search Dropdown Clipping Hotfix

Context: The Grade Search `Course/Class/Block` dropdown was clipped inside the filter panel.

Current state:

- Changed the Grade Search filter panel overflow from hidden to visible.
- Bumped the stylesheet cache key to `styles.css?v=202605281045`.
- Deployed the hotfix to home lab WEB001.
- AWS migration carry-forward: deploy AWS WEB001 from the latest branch so this dropdown clipping fix is included before go-live validation.

Validation:

- `git diff --check` passed with only LF/CRLF warnings.
- Apache configtest returned `Syntax OK`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `styles.css?v=202605281045` and `app.js?v=202605281030`.
- Served CSS contains the Grade Search overflow visibility fix.
- WEB001 deployed SHA-256 hashes matched local `web/index.html` and `web/styles.css`.

Rollback:

- WEB001 rollback: `/home/debian/rollback/hsm/web-grade-search-dropdown-clip-202605281045/web001/web.tgz`
