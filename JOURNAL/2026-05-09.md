# Journal - 2026-05-09

## Modern Preview KPI Polish

- Replaced the Overview Instruction Days dial card with the same projected instruction card treatment used by Projected Instruction Hours.
- Added Instruction Days status, note, shield icon, Required Days, and Projected Days metrics.
- Reused the existing Required Instructional Days compliance snapshot so overview and compliance calculations share one source of truth.
- Added a dedicated selector class for the Projected Instruction Hours card so the Instruction Days and Instruction Hours KPI status updates do not conflict.
- Published updated `web/index.html`, `web/app.js`, and `web/styles.css` to WEB001 `/var/www/home-school-management/web/modern-preview/`.
- Verified public preview cache keys:
  - `styles.css?v=202605091519`
  - `app.js?v=202605091519`
- Validation run:
  - `node --check web/app.js`
  - `git diff --check`

## Notes For Next Session

- Live tenant app was not replaced.
- Existing untracked `tmp/` and icon files were left untouched.
- Continue preview-only workflow unless live cutover is explicitly approved.

## Required Subjects

- Added a `required` flag to Subjects, including PostgreSQL persistence and migration `026_subject_required_flag.sql`.
- Updated Curriculum > Subjects so new subjects can be marked Required and existing subjects can be toggled Required/Optional.
- Added required-subject visibility to student enrollment, including missing required subject rows when a student has no enrolled course/class for a required subject.
- Added Required labels to subject-level dashboard grade and instructional-hours rows.
- Deployed backend updates to APP001, applied the subject required migration across tenant schemas, and restarted `hsm-api.service`.
- Fixed the deployed service path after the initial deploy left the old subject normalizer active.
- Published updated modern preview assets to WEB001 `/var/www/home-school-management/web/modern-preview/`.
- Refined the Subjects form layout so the Subject field is about half width and the Add Subject button sits on a second row aligned left.
- Verified public preview cache keys:
  - `styles.css?v=202605091712`
  - `app.js?v=202605091712`
- Validation run:
  - `node --check web/app.js`
  - Node syntax checks for changed backend JavaScript files
  - `git diff --check`
  - APP001 local health
  - public hosted health

## Dashboard Refinement

- Reworked Overview Attendance and Running Grade Average from semicircle gauges into the same status-card style as Instruction Days and Projected Instruction Hours.
- Replaced the School Year-to-Date Pace overview card with Grades at Risk, including Average Grade Risk and Single Grade Risk counts.
- Added Grade Search value filtering so Single Grade Risks can link into filtered grade results.
- Linked Average Grade Risk to the Performance Course Watchlist and aligned the Overview risk count with that watchlist threshold.
- Refined Performance dashboard tables to fit without internal horizontal scrolling.
- Added Compliance subtabs for Instructional Hours, Instructional Days, and Other.
- Built the Instructional Days compliance panel to mirror Required Instructional Hours, including current pace, projection, key numbers, progress chart, and student breakdown wiring.
- Moved Student Attendance and Instructional Days charts into the Instructional Days compliance tab.
- Tuned Completed Today filters and required-hours/days chart sizing.
- Validation run:
  - `node --check web/app.js`
  - `git diff --check`
- Deployment note:
  - Most preview updates were published during the session.
  - Final Required Instructional Days student breakdown change is committed locally but not deployed because WEB001 / Proxmox host networking became unreachable.
