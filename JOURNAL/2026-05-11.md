# Journal - 2026-05-11

## Dashboard Visibility Configuration Pass

- Updated Administration > Workspace Configuration > Dashboard Visibility to mirror the current Dashboard structure:
  - Overview
  - Execution
  - Performance
  - Compliance / Instructional Hours
  - Compliance / Instructional Days
  - Compliance / Required Subjects
- Added individual visibility flags for current Overview gauges and summary cards.
- Split previously bundled Performance controls so Student Grade Trending and Instructor Grade Trending can be configured separately.
- Split previously bundled Compliance controls so Required Instructional Hours, Required Instructional Days, Instruction Hours Trending, and Instruction Days Trending can be configured separately.
- Added a configurable Required Subject Compliance flag.
- Removed legacy Dashboard Visibility controls that no longer mapped cleanly to current dashboard sections.
- Added backward-compatible client config normalization from old bundled flag names.
- Fixed hosted backend workspace-config normalization so newly split Dashboard Visibility flags persist when unchecked after Save Configuration.
- Deployed updated web assets to WEB001 `/modern-design/` and `/modern-preview/`.
- Deployed `server/src/services/workspace-config-service.js` to APP001 and restarted `hsm-api.service`.
- Verified public preview cache keys:
  - `styles.css?v=202605110902`
  - `app.js?v=202605110902`
- Validation run:
  - `node --check web/app.js`
  - `node --check server/src/services/workspace-config-service.js`
  - `git diff --check`
  - public preview curl checks for Dashboard Visibility labels, updated cache keys, and new dashboard config flags
  - APP001 API health check returned `{"ok":true}`
  - remote normalization check confirmed unchecked dashboard visibility values stay false

## Printable Report Style Pass

- Restyled generated Student and Instructor printable reports with a branded Navigrader report frame.
- Added the current website header logo (`assets/Mitchell_Logo.png`) to report pages.
- Added generated timestamp metadata to the report header.
- Added a report footer promoting `https://www.navigrader.com`.
- Updated Student reports with executive summary cards, student chips, polished tables, grade badges, section headings, and print-aware page footers.
- Updated Instructor reports to use the same branded frame, summary cards, polished tables, and footer treatment.
- Deployed updated `web/index.html` and `web/app.js` to WEB001 `/modern-design/` and `/modern-preview/`.
- Verified public preview cache keys:
  - `styles.css?v=202605110941`
  - `app.js?v=202605110941`
- Validation run:
  - `node --check web/app.js`
  - `git diff --check`
  - public preview curl checks for report logo path, Navigrader URL, report footer, Student Academic Report text, and updated cache keys

## School Day Status And Dashboard Execution Pass

- Added a persisted Scheduled / Completed / Excused instruction status model for School Day actual instructional minutes.
- Added Postgres migration `027_instruction_actual_status.sql` and updated records API persistence so status stays backward-compatible with the existing completed flag.
- Fixed the hosted migration rollout so every tenant schema with `actual_instruction_minutes` receives the new `status` column, including `tenant_mitchell_family`.
- Replaced School Day completion checkboxes with a polished status dropdown.
- Tuned Excused behavior so excused rows remain visible, do not count as open or past due, do not reserve schedule/resource time, and do not block later flexible classes.
- Moved excused rows to the end of the active schedule list while preserving their planned time in-row.
- Preserved configured between-class gaps when active classes move after an excused/cancelled class.
- Added a School Day Status filter for Scheduled, Completed, and Excused.
- Added Dashboard > Execution Class Status with Scheduled, Completed, and Excused links into School Day.
- Added Dashboard > Execution Past Due and Dashboard > Overview Open Classes tracking from 05/12/2026 forward.
- Fixed Flex Block rows so their Edit action renders in the Actions column after the new Status column.
- Standardized preview source-of-truth on `https://mitchell.navigrader.com/modern-preview/`; `modern-design` currently contains an identical duplicate and was not deleted.
- Deployed updated web assets to WEB001 `/modern-preview/` and matching duplicate `/modern-design/`.
- Deployed updated records service/repository files and migration to APP001, reran Postgres migrations, and restarted `hsm-api.service`.
- Verified public preview cache keys:
  - `styles.css?v=202605112000`
  - `app.js?v=202605112000`
- Validation run:
  - `node --check web/app.js`
  - `node --check server/src/services/records-service.js`
  - `node --check server/src/repositories/postgres/records-repository.js`
  - `git diff --check`
  - APP001 API health check returned `{"ok":true}`
  - tenant schema query confirmed `actual_instruction_minutes.status` exists for all tenant schemas
  - public preview curl checks confirmed updated cache keys, School Day Status filter, Class Status/Open Classes script hooks, and Flex Block row status-cell alignment

## SaaS Landing Preview Redesign

- Started the SaaS landing page modernization on branch `saas-modern-redesign`.
- Fast-forwarded the branch to the modern production app baseline so the marketing work starts from the current deployed state.
- Added preview-only files `web/saas-preview.html` and `web/saas-preview.css`.
- Kept live `https://www.navigrader.com/saas.html` untouched while publishing the preview to `https://www.navigrader.com/saas-preview.html`.
- Preserved the existing `web/saas.js` checkout and public plan-loading contract.
- Added `web/assets/saas-hero-family.png` for the hero image.
- Added `web/assets/ModernScreenshot1.png` for the app screenshot.
- Updated the preview hero so the family image fades and overlaps the hero composition.
- Updated preview product imagery to use the modern dashboard screenshot.

## SaaS Preview Validation

- `node --check web/saas.js`
- `git diff --check -- web/saas-preview.html web/saas-preview.css`
- Verified preview HTML references `saas-preview.css?v=202605120130`, `saas-hero-family.png`, and `ModernScreenshot1.png`.
- Verified live `saas.html` still references `saas.css?v=202605051430`.
- Captured desktop and mobile Playwright screenshots for the preview.

## SaaS Preview Commits

- `75533f1 Add SaaS landing preview redesign`
- `781fffd Update SaaS preview hero imagery`
