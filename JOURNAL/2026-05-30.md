# Journal - 2026-05-30

## AWS Mitchell Cutover Preparation

Context: Continue AWS production DNS planning for the first live tenant cutover, using `mitchell.navigrader.com` as the initial target while leaving public traffic on home lab until the go-live window.

Current state:

- GoDaddy explicit CNAME `mitchell -> navigrader.ddns.net` is staged with TTL 600.
- AWS control-plane tenant domain alias `mitchell.navigrader.com` was added for restored tenant `mitchell-aws-validation`.
- Forced AWS setup-status for `https://mitchell.navigrader.com` returns initialized when resolved to `18.188.35.157`.
- AWS TLS certificate `aws-validation.navigrader.com` was expanded to include `mitchell.navigrader.com`.
- Forced AWS HTTPS for `mitchell.navigrader.com` validates without `-k`.
- Temporary manual Certbot hook files were removed after issuance.

Validation:

- GoDaddy authoritative nameservers returned the ACME TXT challenge.
- Certbot issued the expanded certificate, valid through 2026-08-29.
- Apache configtest returned `Syntax OK`; Apache reload succeeded.
- AWS WEB001 served a certificate with SANs for `aws-validation.navigrader.com`, `aws1.navigrader.com`, `mitchell-aws-validation.navigrader.com`, and `mitchell.navigrader.com`.
- `curl --noproxy "*" --resolve mitchell.navigrader.com:443:18.188.35.157 https://mitchell.navigrader.com/api/setup/status` returned HTTP 200 and `{"initialized":true}`.

Rollback and cleanup pointers:

- Mitchell live domain alias rollback: `/home/admin/rollback/hsm/aws-live-mitchell-domain-20260530202356/sql001/control-domain-before.sql`
- Mitchell live TLS rollback: `/home/admin/rollback/hsm/mitchell-live-tls-20260530202600/web001/`

Next pickup:

- Full cutover is intentionally paused while non-technical prep is completed.
- After prep, choose the go-live rehearsal and rollback decision window.
- During the window, do final Mitchell data sync, create the managed NAT Gateway, and validate APP001 Stripe/Postmark egress.
- Update Mitchell AWS app base URL/primary domain to `https://mitchell.navigrader.com`, then replace the GoDaddy `mitchell` CNAME with an A record to `18.188.35.157`.
- After public DNS points to AWS, convert cert renewal from manual DNS challenge to Apache/HTTP validation.
