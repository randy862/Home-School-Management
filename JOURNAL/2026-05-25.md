# Journal - 2026-05-25

## Dashboard Grade Status Rounding

- Fixed a Dashboard status mismatch where a Running Grade Average displayed as `90.0%` could still show `Satisfactory` because the status threshold used the unrounded raw value.
- Added `roundedStatusPercent` so Running Grade Average and Attendance status cards classify against the same one-decimal value displayed to users.
- Bumped tenant app to `app.js?v=202605251234`.
- Deployed `web/index.html` and `web/app.js` to the served WEB001 web root.
- Rollback: `/var/www/home-school-management/rollback/web-dashboard-status-rounded-threshold-202605251234.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check`
  - WEB001 local root references `app.js?v=202605251234`.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605251234`.
  - Served tenant app JS contains `roundedStatusPercent`.
  - Public `https://mitchell.navigrader.com/health` returned HTTP 200.

## Application Architecture Documentation

- Added `RUNBOOKS/application-architecture.md`.
- Documented the current hosted topology in practical terms:
  - browser-side responsibilities and boundaries
  - `WEB001` Apache/static/proxy responsibilities
  - `APP001` tenant app API and control API responsibilities
  - `SQL001` PostgreSQL ownership and data areas
  - common flows for login, tenant app use, dashboard, signup/provisioning, account options, and data export
  - release/validation model and current architecture notes for future cloud cutover

## AWS Budget Migration Documentation

- Added `RUNBOOKS/aws-budget-migration.md`.
- Documented the proof-of-concept AWS launch plan that preserves the lab mental model:
  - `WEB001` as Debian/Apache/static/proxy public entrypoint
  - `APP001` as Debian/Node tenant API and control API host
  - `SQL001` as Debian/PostgreSQL host
  - `MAINT001` as a small Debian maintenance/jumpbox/Codex-capable node
- Included phases for AWS foundation, base server build, database migration, app/web deployment, DNS/TLS, validation, and cutover.
- Included budget backup/logging guidance, cost-control levers, known tradeoffs, future upgrade path, and go/no-go criteria.

## Help Center Account Article Production Deploy

- Updated the Help Center `Account, Billing, and Data Export` article using `C:\Users\rmitchell\Downloads\navigrader_help_center_account.md`.
- Replaced the short Help Center bullet article with detailed guidance for:
  - Account Menu options
  - View Account
  - Account Options
  - Dormant Mode
  - Data Export
  - subscription cancellation
  - Change Password and Sign Out
  - common account/billing/export questions
- Removed draft placeholder review notes from the downloaded source before publishing.
- Bumped tenant app to `app.js?v=202605252122`.
- Deployed `web/index.html` and `web/app.js` to WEB001.
- Rollback: `/var/www/home-school-management/rollback/web-help-account-202605252122.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check`
  - Apache config test passed on WEB001.
  - Apache reload completed and `apache2` remained active.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605252122`.
  - Served `https://mitchell.navigrader.com/app.js?v=202605252122` contains `Account Menu Options`, `Dormant Mode is billed at`, `Request Data Export`, and `Keep Subscription Active`.
  - Public `https://mitchell.navigrader.com/health` returned HTTP 200 with `{"ok":true}`.
  - Full hosted release gate passed from the user's PowerShell session against `https://mitchell.navigrader.com`.
