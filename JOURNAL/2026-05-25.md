# Journal - 2026-05-25

## Dashboard Grade Status Rounding

- Fixed a Dashboard status mismatch where a Running Grade Average displayed as `90.0%` could still show `Satisfactory` because the status threshold used the unrounded raw value.
- Added `roundedStatusPercent` so Running Grade Average and Attendance status cards classify against the same one-decimal value displayed to users.
- Bumped tenant app to `app.js?v=202605251234`.
- Deployed `web/index.html` and `web/app.js` to the served WEB001 web root.
- Rollback: `/var/www/home-school-management/rollback/web-dashboard-status-rounded-threshold-202605251234.tgz`.
- Validation:
  - `node --check web/app.js`
  - `git diff --check`
  - WEB001 local root references `app.js?v=202605251234`.
  - Public `mitchell` and `smoketest` tenant roots reference `app.js?v=202605251234`.
  - Served tenant app JS contains `roundedStatusPercent`.
  - Public `https://mitchell.navigrader.com/health` returned HTTP 200.
