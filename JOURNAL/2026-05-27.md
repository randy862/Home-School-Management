# 2026-05-27

## Home Lab Password Recovery

Context: Completed the forgot-password capability and compact login refresh for the home lab production tenant at `https://mitchell.navigrader.com/`.

Current state:

- Added tenant forgot-password request and reset-complete screens.
- Added password reset API endpoints, token persistence, token consumption, and session revocation after reset.
- Added Postmark-backed tenant mail support with `log_only`, `allowlist_only`, and `real_send` modes.
- Added migration `032_password_reset_tokens.sql`.
- Compact login UI refresh is deployed with the password recovery screens.
- Administrator users require an email address in both the UI and backend API; student users may omit email.
- Home lab APP001 and WEB001 deployed file hashes match the local working tree for the password-recovery files.
- User completed the hosted email reset flow and signed in successfully.

Validation:

- APP001 deployed JS syntax checks passed with `node --check`.
- Migration `032_password_reset_tokens.sql` applied successfully.
- APP001 `hsm-api.service` restarted and returned local `/health` as `{"ok":true}`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `app.js?v=202605271529` and `styles.css?v=202605271537`.
- Public reset request for `admin` returned generic success.
- Postmark showed the hosted password reset email as `Sent`.
- User used the email link, reset the password, and logged into `mitchell.navigrader.com`.

Rollback:

- APP001 source/env backup: `/home/debian/rollback/hsm/forgot-password-20260527161035/app001/`
- WEB001 web rollback: `/var/www/home-school-management/rollback/web-forgot-password-20260527161035.tgz`

Risks:

- Keep Postmark, database, Stripe, smoke credentials, and runtime env files out of the repo.
- Untracked scratch screenshots/icons and `tmp/` remain local and should stay out of the password-recovery commit.

## Home Lab Attendance And School Day Filters

Context: Polished search/filter workflows in the home lab production tenant.

Current state:

- Attendance Search now uses `Start Date` and `End Date` filters, matching Grade Search behavior.
- School Day Filters now use one grouped `Scheduled Item` filter for Classes, Courses, and Schedule Blocks.
- The School Day Scheduled Item dropdown uses compact two-line option rows with a wider menu so class names and metadata do not overlap.
- WEB001 is deployed with `app.js?v=202605271700` and `styles.css?v=202605271700`.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `app.js?v=202605271700` and `styles.css?v=202605271700`.
- Served app script contains `attendance-filter-start-date`, `attendance-filter-end-date`, `school-day-scheduled-item-checkbox`, and `school-day-scheduled-option`.
- WEB001 deployed SHA-256 hashes matched local `web/index.html`, `web/app.js`, and `web/styles.css`.

Rollback:

- Attendance Search rollback: `/var/www/home-school-management/rollback/web-attendance-date-range-202605271635.tgz`
- School Day Scheduled Item rollback: `/var/www/home-school-management/rollback/web-school-day-scheduled-item-filter-202605271655.tgz`
- School Day dropdown cleanup rollback: `/var/www/home-school-management/rollback/web-school-day-scheduled-item-cleanup-202605271700.tgz`

## Home Lab Open Items Gauge

Context: Aligned the Open Items Today gauge with its four visible action buckets on `https://mitchell.navigrader.com/`.

Current state:

- Removed Past Due from the Open Items Today total.
- Removed Past Due from the Open Items Today summary text.
- Removed the Past Due chip from the Open Items detail grid.
- WEB001 is deployed with `app.js?v=202605271851` and `styles.css?v=202605271700`.
- Code committed as `93a6ab8 Align open items count with visible buckets`.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed.
- WEB001 deployed SHA-256 hashes matched local `web/index.html` and `web/app.js`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `app.js?v=202605271851`.
- Served app script excludes Past Due from the Open Items Today total and summary copy.

Rollback:

- WEB001 rollback: `/var/www/home-school-management/rollback/web-open-items-gauge-202605271851.tgz`

## Home Lab Past Due Schedule Items Gauge

Context: Added a dedicated Past Due Schedule Items card to the Open Items Today detail gauge row on `https://mitchell.navigrader.com/`.

Current state:

- Open Items Today detail row now includes a `Past Due Schedule Items` gauge.
- The new gauge links to the School Day `past-due` quick filter.
- The overview Open Items Today total and summary text still exclude Past Due.
- WEB001 is deployed with `app.js?v=202605271906` and `styles.css?v=202605271700`.
- Code committed as `4f75edc Add past due schedule items gauge`.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed.
- WEB001 deployed SHA-256 hashes matched local `web/index.html` and `web/app.js`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `app.js?v=202605271906`.
- Served app script contains `Past Due Schedule Items` and the `past-due` quick filter.

Rollback:

- WEB001 rollback: `/var/www/home-school-management/rollback/web-past-due-schedule-gauge-202605271906.tgz`

## Home Lab Tenant Password Reset Links

Context: Fixed password reset emails requested from `pj-cool.navigrader.com` incorrectly linking to `mitchell.navigrader.com`.

Current state:

- Password reset links now prefer the resolved tenant runtime URL, then the request host, before the global app base URL.
- Existing wrong-host reset emails should be discarded; request a new reset email after this deploy.
- APP001 is deployed with the tenant-aware reset link fix.
- Code committed as `a1e0810 Use tenant URL for password reset links`.

Validation:

- `node --check server/src/routes/auth-routes.js` passed locally and on APP001.
- Local and deployed URL assertions produced `https://pj-cool.navigrader.com/#resetToken=...` when global config was Mitchell.
- APP001 `hsm-api.service` restarted and returned local `/health` as `{"ok":true}`.
- Public `https://mitchell.navigrader.com/health` and `https://pj-cool.navigrader.com/health` returned HTTP 200.
- Public PJ-Cool password reset request for an unknown user returned the generic success response.

Rollback:

- APP001 route rollback: `/home/debian/rollback/hsm/password-reset-tenant-url-202605272030/app001/server/src/routes/auth-routes.js`

## AWS Migration Carry-Forward

Context: Preserve the home lab checkpoint for the next AWS migration session.

Current state:

- Carry `7031068 Add tenant password reset flow`, `18a482f Polish attendance and school day filters`, `93a6ab8 Align open items count with visible buckets`, `4f75edc Add past due schedule items gauge`, and `a1e0810 Use tenant URL for password reset links` into AWS.
- AWS APP001 needs the password reset backend/API/mail changes from `7031068` and tenant-aware reset link fix from `a1e0810`.
- AWS SQL001 needs migration `032_password_reset_tokens.sql` after the database restore/tenant DB is ready.
- AWS WEB001 needs web assets through `4f75edc`, including `app.js?v=202605271906` and `styles.css?v=202605271700`.
- AWS runtime env must set `PUBLIC_APP_BASE_URL` to the AWS domain and configure tenant mail/Postmark values.

Next action:

- Resume AWS migration by deploying/pulling `saas-modern-redesign` through `a1e0810` or later, then smoke health, reset email tenant URL, login, Attendance Search, School Day Scheduled Item filtering, Open Items Today gauge, and Past Due Schedule Items gauge.
