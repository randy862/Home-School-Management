# 2026-05-27

## Home Lab Password Recovery

Context: Completed the forgot-password capability and compact login refresh for the home lab production tenant at `https://mitchell.navigrader.com/`.

Current state:

- Added tenant forgot-password request and reset-complete screens.
- Added password reset API endpoints, token persistence, token consumption, and session revocation after reset.
- Added Postmark-backed tenant mail support with `log_only`, `allowlist_only`, and `real_send` modes.
- Added migration `032_password_reset_tokens.sql`.
- Compact login UI refresh is deployed with the password recovery screens.
- Administrator users require an email address in both the UI and backend API; student users may omit email.
- Home lab APP001 and WEB001 deployed file hashes match the local working tree for the password-recovery files.
- User completed the hosted email reset flow and signed in successfully.

Validation:

- APP001 deployed JS syntax checks passed with `node --check`.
- Migration `032_password_reset_tokens.sql` applied successfully.
- APP001 `hsm-api.service` restarted and returned local `/health` as `{"ok":true}`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `app.js?v=202605271529` and `styles.css?v=202605271537`.
- Public reset request for `admin` returned generic success.
- Postmark showed the hosted password reset email as `Sent`.
- User used the email link, reset the password, and logged into `mitchell.navigrader.com`.

Rollback:

- APP001 source/env backup: `/home/debian/rollback/hsm/forgot-password-20260527161035/app001/`
- WEB001 web rollback: `/var/www/home-school-management/rollback/web-forgot-password-20260527161035.tgz`

Risks:

- Keep Postmark, database, Stripe, smoke credentials, and runtime env files out of the repo.
- Untracked scratch screenshots/icons and `tmp/` remain local and should stay out of the password-recovery commit.

## Home Lab Attendance And School Day Filters

Context: Polished search/filter workflows in the home lab production tenant.

Current state:

- Attendance Search now uses `Start Date` and `End Date` filters, matching Grade Search behavior.
- School Day Filters now use one grouped `Scheduled Item` filter for Classes, Courses, and Schedule Blocks.
- The School Day Scheduled Item dropdown uses compact two-line option rows with a wider menu so class names and metadata do not overlap.
- WEB001 is deployed with `app.js?v=202605271700` and `styles.css?v=202605271700`.

Validation:

- `node --check web/app.js` passed.
- `git diff --check` passed.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.
- Public root references `app.js?v=202605271700` and `styles.css?v=202605271700`.
- Served app script contains `attendance-filter-start-date`, `attendance-filter-end-date`, `school-day-scheduled-item-checkbox`, and `school-day-scheduled-option`.
- WEB001 deployed SHA-256 hashes matched local `web/index.html`, `web/app.js`, and `web/styles.css`.

Rollback:

- Attendance Search rollback: `/var/www/home-school-management/rollback/web-attendance-date-range-202605271635.tgz`
- School Day Scheduled Item rollback: `/var/www/home-school-management/rollback/web-school-day-scheduled-item-filter-202605271655.tgz`
- School Day dropdown cleanup rollback: `/var/www/home-school-management/rollback/web-school-day-scheduled-item-cleanup-202605271700.tgz`

## AWS Migration Carry-Forward

Context: Preserve the home lab checkpoint for the next AWS migration session.

Current state:

- Carry `7031068 Add tenant password reset flow` and `18a482f Polish attendance and school day filters` into AWS.
- AWS APP001 needs the password reset backend/API/mail changes from `7031068`.
- AWS SQL001 needs migration `032_password_reset_tokens.sql` after the database restore/tenant DB is ready.
- AWS WEB001 needs web assets through `18a482f`, including `app.js?v=202605271700` and `styles.css?v=202605271700`.
- AWS runtime env must set `PUBLIC_APP_BASE_URL` to the AWS domain and configure tenant mail/Postmark values.

Next action:

- Resume AWS migration by deploying/pulling `saas-modern-redesign` through `18a482f`, then smoke health, reset email, login, Attendance Search, and School Day Scheduled Item filtering.
