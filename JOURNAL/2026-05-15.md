# 2026-05-15

## Global Consistency Production Release

- Promoted the approved global consistency polish bundle to WEB001.
- Production code commit:
  - `34743f5 Polish workspace consistency surfaces`
- Live web assets:
  - `styles.css?v=202605150849`
  - `app.js?v=202605150849`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-202605150849.tgz`

## Release Scope

- Students now has a compact page header with active, required-gap, and archived summary chips.
- Student Detail now has a compact overview with scheduled-item, required-subject, grade-average, and absence chips.
- Curriculum now has compact page and setup-section headings with live badges for subjects, courses/classes, grade types, and grading criteria.
- Schedule now has compact page and setup-section headings with live badges for school year, quarters, school day settings, schedule blocks, and holidays.
- Shared responsive CSS keeps the new page headers, setup badges, and Student Detail overview readable on narrow screens.

## Validation

- `node --check web/app.js`
- `git diff --check -- web/app.js web/index.html web/styles.css STATUS.md HANDOFF.md`
- PowerShell parser checks passed for `scripts/Test-HostedSmoke.ps1` and `scripts/Invoke-HostedReleaseGate.ps1`
- Local Web Preview returned `200` for `http://127.0.0.1:5500/?seedPreview=1`
- WEB001 rollback snapshot created before deployment
- WEB001 Apache config syntax OK and Apache service active
- Remote WEB001 hashes matched local `web/index.html`, `web/app.js`, and `web/styles.css`
- Public health endpoint returned `{"ok":true}` for `https://mitchell.navigrader.com/health`
- Live HTML references `styles.css?v=202605150849` and `app.js?v=202605150849`

## Notes

- Authenticated hosted smoke was not run because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` were not set in this shell.
- Untracked local scratch screenshots/icons and `tmp/` remain outside the release commits.

## Operational Tab Styling Release

- Promoted the approved operational tab styling polish to WEB001.
- Production code commit:
  - `677417b Polish operational tab styling`
- Live web assets:
  - `styles.css?v=202605150913`
  - `app.js?v=202605150849`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-202605150913.tgz`

## Tab Styling Scope

- Dashboard primary tabs now use the flat underline tab style used by School Day.
- Dashboard Compliance sub-tabs now use the same flat underline treatment instead of the filled pill style.
- Grades and Attendance tabs now use the same flat underline treatment.
- Calendar, Administration, Curriculum, and Schedule setup/config tabs were intentionally left unchanged.

## Tab Styling Validation

- `node --check web/app.js`
- `git diff --check -- web/index.html web/styles.css`
- PowerShell parser checks passed for `scripts/Test-HostedSmoke.ps1` and `scripts/Invoke-HostedReleaseGate.ps1`
- Local Web Preview returned `200` for `http://127.0.0.1:5500/?seedPreview=1`
- WEB001 rollback snapshot created before deployment
- WEB001 Apache config syntax OK and Apache service active
- Remote WEB001 hashes matched local `web/index.html` and `web/styles.css`
- Public health endpoint returned `{"ok":true}` for `https://mitchell.navigrader.com/health`
- Live HTML references `styles.css?v=202605150913` and `app.js?v=202605150849`
- Authenticated hosted smoke was not run because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` were not set in this shell.

## Account Options Copy Release

- Promoted the approved Account Options wording polish to WEB001.
- Production code commit:
  - `c042e07 Polish account options copy`
- Live web assets:
  - `styles.css?v=202605150913`
  - `app.js?v=202605150933`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-202605150933.tgz`

## Account Options Scope

- Replaced internal terms around tenant lifecycle, lower-frequency actions, runtime activity, and offboarding.
- Dormant Mode copy now focuses on inactive periods, retained records/settings/history, and later reactivation.
- Reactivation copy now says the account returns to active use and regular billing/features.
- Data Export copy now explains downloading homeschool records for files, backup, or transfer.
- Confirmation prompts were updated with the same parent-facing language.

## Account Options Validation

- `node --check web/app.js`
- `git diff --check -- web/index.html web/app.js`
- PowerShell parser checks passed for `scripts/Test-HostedSmoke.ps1` and `scripts/Invoke-HostedReleaseGate.ps1`
- Local Web Preview returned `200` for `http://127.0.0.1:5500/?seedPreview=1`
- WEB001 rollback snapshot created before deployment
- WEB001 Apache config syntax OK and Apache service active
- Remote WEB001 hashes matched local `web/index.html` and `web/app.js`
- Public health endpoint returned `{"ok":true}` for `https://mitchell.navigrader.com/health`
- Live HTML references `styles.css?v=202605150913` and `app.js?v=202605150933`
- Authenticated hosted smoke was not run because `HSM_HOSTED_SMOKE_USERNAME` and `HSM_HOSTED_SMOKE_PASSWORD` were not set in this shell.

## Public SaaS Site Screenshot/Footer Release

- Promoted the approved `www.navigrader.com` public-site refresh to WEB001.
- Production code commit:
  - `8894a65 Refresh public site screenshots and footer`
- Live public asset cache key:
  - `saas-polish.css?v=202605151005`
- Latest rollback snapshot:
  `/var/www/home-school-management/rollback/web-202605151005.tgz`

## Public Site Scope

- Replaced the bottom `Free yourself to Enjoy Homeschool` section screenshots with five refreshed UI polish screenshots.
- Added a Contact nav target.
- Added footer contact email:
  - `support@navigrader.com`
- Added copyright branding:
  - `Copyright 2026, Navigrader, LLC`

## Public Site Validation

- `git diff --check -- web/saas.html web/saas-polish.css`
- Local Web Preview returned `200` for `http://127.0.0.1:5500/saas.html`
- Local preview included the new CSS key, support email, copyright line, and refreshed screenshot references.
- WEB001 rollback snapshot created before deployment.
- WEB001 Apache config syntax OK and Apache service active.
- Remote WEB001 hashes matched local `web/saas.html`, `web/saas-polish.css`, and the five new public screenshot assets.
- Public `https://www.navigrader.com/` returned HTTP 200 and includes the new CSS key, support email, copyright line, and refreshed screenshot references.
- All five new public screenshot assets returned HTTP 200.
- Browser render check was skipped because Playwright is not installed in this workspace.
