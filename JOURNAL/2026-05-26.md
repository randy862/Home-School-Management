# 2026-05-26

## AWS Migration Checkpoint

Context: Continued moving Navigrader into the low-cost AWS layout in `us-east-2`.

Current state:

- `WEB001`, `APP001`, and `SQL001` are running and reachable through the planned public/private topology.
- `APP001` has tracked app source, production dependencies, root-owned runtime env files, and systemd services installed.
- `hsm-api.service` and `hsm-control-api.service` are active on `APP001`.
- Tenant PostgreSQL migrations completed through `031_independent_learning_instructor.sql`.
- Control PostgreSQL migrations completed through `012_export_cleanup_indexes.sql`.
- `WEB001` static tenant/control assets and Apache HTTP vhost are deployed.
- Public AWS checks pass at `http://18.188.35.157/`, `/terms/`, `/control/`, `/health`, and `/control-api/health`.
- `TEMP-NAT` was recreated for private package downloads, then removed again after dependency installation.
- `APP001` outbound internet now times out as expected, restoring the no-general-internet private subnet posture.
- Validation host `aws-validation.navigrader.com` maps in the AWS control database to tenant `tenant-aws-validation`, environment `env-aws-validation-production`, and schema `tenant_aws_validation`.
- Host-header validation through AWS `WEB001` resolves `aws-validation.navigrader.com` and returns tenant setup status.

Validation:

- `APP001` local `/health` returned `{"ok":true}` on ports `3000` and `3100`.
- AWS public `/health` and `/control-api/health` returned `{"ok":true}`.
- Host-header request for `aws-validation.navigrader.com` returned `/api/setup/status` as `{"initialized":false}`.
- Host-header request for `/api/me` returned `401 Authentication required`, which is expected before login.
- Control runtime resolve endpoint returned the expected tenant/environment mapping.

Resume point:

1. Point DNS or a local hosts-file entry for `aws-validation.navigrader.com` to `18.188.35.157`.
2. Decide whether to initialize the AWS validation tenant with a temporary admin or wait for production data restore.
3. Verify the first scheduled DLM-created EBS snapshots after the next policy window.
4. Continue AWS app validation, then DNS/TLS planning.

Risks:

- Do not commit server env files, generated passwords, private keys, smoke credentials, Stripe secrets, or Postmark secrets.
- `aws-validation.navigrader.com` is only mapped in AWS application data until DNS or a hosts-file entry points it to AWS `WEB001`.
- Untracked scratch assets remain local and outside this commit.
