# Journal - 2026-06-01

## AWS Mitchell Final Data Sync

Context: Continue AWS migration toward first live Mitchell cutover while public `mitchell.navigrader.com` still points to home lab.

Current state:

- Source home-lab tenant: `tenant_mitchell_family`.
- AWS target tenant: `tenant_mitchell_aws_validation`.
- Saved AWS rollback artifacts before restore.
- Exported fresh home-lab Mitchell schema, transformed schema name, and restored it to AWS.
- AWS `hsm-api.service` was stopped during schema replacement and restarted after restore.

Validation:

- Source and AWS target table counts matched for all 26 tenant tables.
- AWS target schema owner is `navigrader_app`; all 26 tables are owned by `navigrader_app`.
- `hsm-api.service` and `hsm-control-api.service` are active.
- Forced AWS HTTPS setup-status for `mitchell.navigrader.com` returned initialized.

Rollback:

- `/home/admin/rollback/hsm/aws-final-mitchell-data-sync-20260601120000/sql001/`

Next pickup:

- Create managed NAT Gateway and private route for APP001 egress.
- Validate APP001 can reach Stripe, Postmark, and an external IP endpoint.
- Update Mitchell AWS app base URL/primary domain, then cut over GoDaddy `mitchell` DNS from CNAME to A record `18.188.35.157`.
