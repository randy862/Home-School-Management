# Journal - 2026-07-12

## Class Conflict Year Scope

Context: Home lab validation after school-year rollover testing.

Current state:

- Pam Jones showed a false Basket Weaving conflict while editing the Natural Science class in `2026-2027`.
- Live Mitchell data confirmed Pam had current-year course enrollments but no current-year class-section enrollments; the stale warning came from a prior-year Basket Weaving section enrollment.
- Class conflict checks now filter section enrollments to the active Academic Year.

Validation:

- Local `node --check web/app.js` and `git diff --check` passed.
- WEB001 was deployed with `app.js?v=202607121020`.
- Public `https://mitchell.navigrader.com/health` returned HTTP 200.

Next pickup:

- Include this class conflict year-scope fix in the AWS sync bundle with migrations `033` and `034`.
